# VoxVulgi â€” Task Board

Last updated: 2026-07-31

This is the single source of truth for work status.

## Status legend

- `BACKLOG` â€” not started
- `IN_PROGRESS` â€” actively being worked on
- `BLOCKED` â€” waiting on external input
- `DONE` â€” completed + verified per WP acceptance and `governance/workflow/PROOF_STANDARD.md`
- `SUPERSEDED` â€” direction retired by a later WP; row kept for traceability and points to the replacement

## Work packets

| ID | Title | Status | Owner | Notes |
|---|---|---|---|---|
| WP-0001 | Repo structure: product + governance | DONE | Codex | Repo skeleton + operating docs in place; added reverse-build sanitization script. |
| WP-0002 | Product spec + technical design v0 | DONE | Codex | Drafted specs for cross-platform, local-first vNext. |
| WP-0003 | Downloader design | DONE | Codex | Added provider interface + provenance + UX/safety requirements to `governance/spec/TECHNICAL_DESIGN.md`; follow-up hardening tracked separately. |
| WP-0004 | Voice-preserving dubbing R&D plan | DONE | Codex | R&D plan documented in `governance/spec/VOICE_PRESERVING_DUBBING_RD_PLAN.md`. |
| WP-0005 | Diagnostics + log retention policy | DONE | Codex | Reconciled: policy documented in `governance/spec/TECHNICAL_DESIGN.md` section 6; safe-by-default diagnostics export with redaction. |
| WP-0006 | Phase 1: Product app skeleton | DONE | Codex | Tauri v2 + React app skeleton created under `product/desktop/`; Rust engine boundary in `product/engine/`; build verified. |
| WP-0007 | Phase 1: Local model runtime + model manager | DONE | Codex | Implemented bundled model manifest + hash verification and Diagnostics inventory (local-first; no default cloud). |
| WP-0008 | Phase 1: Library DB + import pipeline | DONE | Codex | SQLite schema + local import (ffprobe metadata + ffmpeg thumbnail) wired into Library UI. |
| WP-0009 | Phase 1: Jobs engine + queue UI | DONE | Codex | Durable jobs + progress UI; cancel/retry; per-job logs/artifacts; log rotation + retention defaults; build + tests verified. |
| WP-0010 | Phase 1: Local ASR (JA/KO) -> captions | DONE | Codex | `asr_local` job (ffmpeg audio extract -> local whisper.cpp -> `source.json/srt/vtt` per item) + model install via Diagnostics; build verified. |
| WP-0011 | Phase 1: Subtitle editor v1 | DONE | Codex | Subtitle editor UI (preview + edit/split/merge/nudge/normalize) + versioned saves (new `subtitle_track` row + new files) + SRT/VTT export; build verified. |
| WP-0012 | Phase 1: Local translate CC (JA/KO -> EN) | DONE | Codex | `translate_local` job (Whisper.cpp translate + alignment), glossary + QC warnings report, minimal bilingual editor view; build verified. |
| WP-0013 | Phase 1: Diagnostics UI + retention tools | DONE | Codex | Diagnostics UI + retention/export tools implemented; verified via `cargo test` + `npm run build`. |
| WP-0014 | Phase 1: Batch URL ingest | DONE | Codex | Added direct-URL batch ingest (up to 1500 entries), queue jobs + provenance table, Library form, and build/test verification. |
| WP-0015 | Phase 1: In-app image archive batch downloader | DONE | Codex | Added in-app blog/forum image batch crawl job (pagination + content links, profile/avatar skip, full-image preference), new Library form + Tauri command, and build/test verification. |
| WP-0016 | Phase 1: YouTube + Instagram downloader | DONE | Codex | Added `yt-dlp` YouTube ingest + Instagram batch ingest (expand to media targets) on top of URL ingest pipeline; backfilled WP + spec sync. |
| WP-0017 | Phase 1: Downloader privacy hardening | DONE | Codex | Verified: engine+tauri `cargo test` + desktop `npm run build`; explicit yt-dlp install + cookies not persisted in job params. |
| WP-0018 | Rebrand to VoxVulgi | DONE | Codex | Renamed project branding, Tauri bundle identifiers, and bootstrap tooling names. |
| WP-0020 | Voice dubbing tooling landscape research (2026) | DONE | Codex | Research local OSS vs service-backed tools; license/weights matrix; recommend default + BYO stacks for Phase 2. |
| WP-0021 | Phase 2: Python toolchain runner + explicit installers | DONE | Codex | Verified: engine+tauri `cargo test` + desktop `npm run build`; Python venv setup/status surfaced in Diagnostics. |
| WP-0022 | Phase 2: Separation (Spleeter pack + job) | DONE | Codex | Explicit-install Spleeter pack + `separate_audio_spleeter` job to produce vocals/background stems. |
| WP-0023 | Phase 2: Diarization baseline (pack + job) | DONE | Codex | Explicit-install diarization pack + `diarize_local_v1` job to add speaker labels to subtitle tracks. |
| WP-0024 | Phase 2: TTS preview (pack + job) | DONE | Codex | Explicit-install TTS preview pack + `tts_preview_pyttsx3_v1` job to synthesize per-segment audio + manifest. |
| WP-0025 | Phase 2: Dub preview mix + export | DONE | Codex | `mix_dub_preview_v1` job overlays TTS segments on separation background stem to produce a preview dub WAV. |
| WP-0026 | Phase 2: Mux dub preview into video | DONE | Codex | `mux_dub_preview_v1` job muxes dub preview audio onto original media to produce a preview MP4. |
| WP-0027 | Phase 2: Verification + packaging hardening (Python packs) | DONE | Codex | Manual smoke chain now executes end-to-end on sample, including Spleeter separation/runtime compatibility (HTTP/2 dependency fix for `httpx`). |
| WP-0028 | Phase 2: Neural TTS baseline (Kokoro or MeloTTS) | DONE | Codex | Added explicit-install neural TTS (Kokoro) pack and `tts_neural_local_v1` preview job producing per-segment WAVs + mixer-compatible manifest. |
| WP-0029 | Phase 2: Voice-preserving dubbing (OpenVoice/CosyVoice) | DONE | Codex | Implemented Kokoro+OpenVoice V2 voice-preserving dub with explicit model download; verified end-to-end through mix+mux on KO multi-speaker sample. |
| WP-0052 | FFmpeg dependency UX (ffprobe missing) | DONE | Codex | Library import preflight + Jobs remediation button for FFmpeg/ffprobe missing; explicit install only (no silent downloads). |
| WP-0053 | UI pipeline coverage (feature discoverability) | DONE | Codex | Subtitle Editor exposes ASR->translate->diarize->TTS/dub->separate->mix->mux plus Outputs (reveal/export MP4); Library keeps quick item actions + Editor entrypoint. |
| WP-0050 | Phase 2: Spleeter install resilience on Windows | DONE | Codex | Make Spleeter installer robust for CPython 3.8+ without requiring incompatible wheels; add explicit remediation guidance. |
| WP-0051 | Phase 2: Spleeter runtime compatibility fallback path | DONE | Codex | Deterministic Spleeter fallback path plus runtime dependency bootstrap added; WP-0027 smoke confirmed passing (including Spleeter runtime compatibility). |
| WP-0030 | ROI-01: One-click "Phase 2 Packs" installer | DONE | Codex | One-click installer job + per-step logs/state + Diagnostics progress UI; verified via `cargo test` + Windows bundle build. |
| WP-0031 | ROI-02: Portable Python distribution option | DONE | Codex | Portable Python status + explicit install + preference order; verified via build + tests. |
| WP-0032 | ROI-05: Single-pass audio mixer | DONE | Codex | Single-pass FFmpeg mixer (ducking + loudnorm) + UI settings + timing-fit; verified via build + tests. |
| WP-0033 | ROI-06: Speaker label management UI | DONE | Codex | Speaker display names + bulk assign/merge + best-effort propagation across tracks; verified via build + tests. |
| WP-0034 | ROI-07: In-app audio preview player (A/B) | DONE | Codex | Artifacts browser + in-app audio preview + quick video source toggle; verified via build + tests. |
| WP-0035 | ROI-08: Dub timing-fit tools | DONE | Codex | Timing-fit controls (enable + min/max) surfaced in UI; report emitted + export pack includes it; verified via build + tests. |
| WP-0036 | ROI-09: Subtitle-to-dub QC report | DONE | Codex | QC report job + Subtitle Editor viewer with jump-to-segment; verified via build + tests. |
| WP-0037 | ROI-10: Optional vocals cleanup | DONE | Codex | Optional vocals cleanup job + UI action + artifact listing; verified via build + tests. |
| WP-0038 | ROI-11: Mux options + metadata | DONE | Codex | Mux container (mp4/mkv) + keep-original + lang tags + UI controls; verified via build + tests. |
| WP-0039 | ROI-12: Batch processing rules on import | DONE | Codex | Batch-on-import rules (local-only; off by default) + Diagnostics UI + Library transparency; verified via build + tests. |
| WP-0040 | ROI-13: Better separation backend option | DONE | Codex | Demucs optional pack + job + UI selection; verified via build + tests. |
| WP-0041 | ROI-14: Better diarization backend option (BYO) | DONE | Codex | Optional BYO diarization backend (pyannote) config + per-job selection; verified via build + tests. |
| WP-0042 | ROI-15: Pack/model integrity | DONE | Codex | Integrity manifest generation + Diagnostics UI; pinned/hash-verified downloads where applicable; verified via build + tests. |
| WP-0043 | ROI-16: Derived output browser | DONE | Codex | Per-item artifacts list with reveal/open/rerun + job log access; verified via build + tests. |
| WP-0044 | ROI-17: Export packs | DONE | Codex | Export pack job (zip) + UI entrypoints; verified via build + tests. |
| WP-0045 | ROI-18: Performance tiering | DONE | Codex | Diagnostics shows tier + recommended settings (best-effort); verified via build + tests. |
| WP-0046 | ROI-19: Crash-safe resumable external steps | DONE | Codex | Best-effort resume/skip behavior across external steps; verified via build + tests. |
| WP-0047 | ROI-20: Licensing/attribution report generator | DONE | Codex | Licensing report generator + Diagnostics UI reveal; verified via build + tests. |
| WP-0048 | Phase 2: Speaker -> TTS voice mapping UI (pyttsx3) | DONE | Codex | Persist per-item speaker settings and select per-speaker system TTS voice; feed mapping into `tts_preview_pyttsx3_v1`. |
| WP-0019 | Fix mojibake encoding artifacts | DONE | Codex | Replaced mis-encoded punctuation sequences (e.g., `Ã¢â‚¬â€`, `Ã¢â‚¬Å“`) in docs/UI; kept Rust markers ASCII via Unicode escapes. |
| WP-0054 | Offline full bundle (Phase 1 + Phase 2 dependencies) | DONE | Codex | Bundled Phase 1+2 deps into installer as offline `payload.zip` + `manifest.json`; bootstrap extracts into app-data on first run (no downloads). |
| WP-0055 | UI persistence + output/artifact path visibility | DONE | Codex | Persist key UI settings across panes and make output/artifact folders easy to open from Jobs/Library; update specs for bundled-deps stance. |
| WP-0056 | Phase 1: YouTube subscriptions data model + engine flow | DONE | Codex | Added `youtube_subscription` schema + engine CRUD/queue/export/import; queueing applies per-subscription folder maps under downloads/video/subscriptions (or output override). |
| WP-0057 | Phase 1: YouTube subscriptions UI + portability | DONE | Codex | Added Library subscription UI (save/edit/delete, queue one/all, JSON export/import) with Tauri command wiring and pane-switch persistence via DB reload. |
| WP-0058 | Phase 1: Subscription interval + download responsiveness hardening | DONE | Codex | Added per-subscription refresh interval input/persistence (including import/export) and removed enqueue/startup blocking paths; verified via engine/tauri tests + desktop build. |
| WP-0059 | 4KVDP migration: import subscriptions + per-subscription dedupe | DONE | Codex | Import 4KVDP exports (`subscriptions.json` + optional `subscription_entries.csv`), preserve folder intent/output overrides, seed yt-dlp archive to avoid re-downloads, and queue per-video refresh jobs (verified via engine+tauri tests + desktop build). |
| WP-0060 | Phase 1: Safe Mode startup (recovery mode) | DONE | Codex | Added safe-mode startup path + persistent flag; Safe Mode skips heavy startup work and pauses queue with explicit UI controls. |
| WP-0061 | Thumbnail disk cache + Library virtualization | DONE | Codex | Added bounded `cache/thumbs` thumbnail cache (eviction + diagnostics clear/status) and virtualized Library rendering for large datasets. |
| WP-0062 | Subscription groups/tags + failure backoff | DONE | Codex | Added subscription groups, membership assignment/filtering, group queueing, and per-subscription exponential failure backoff gating. |
| WP-0063 | Output templates + â€œSmart presetsâ€ | DONE | Codex | Added reusable download presets (default + per-subscription override), template-based output naming, and preset import/export flows. |
| WP-0064 | Migration scan + optional index-only library import | DONE | Codex | Added folder scan to seed yt-dlp archives and optional index-only import of existing downloads into Library DB (non-destructive). |
| WP-0065 | Jobs "Open outputs" ACL fix | DONE | Codex | Added opener ACL permission + shared safe path opener with `open_path`/`reveal_item_in_dir` fallback and copy-path guidance across windows. |
| WP-0066 | Localization Studio first + window architecture refresh | DONE | Codex | Localization Studio is now the first/default workspace; navigation split into Video Archiver, Instagram Archiver, Image Archive, Media Library, Jobs/Queue, Diagnostics. |
| WP-0067 | Window switch freeze reduction | DONE | Codex | Replaced mount-all hidden-page approach with lazy-loaded single-active window rendering to reduce switch stalls/background work. |
| WP-0068 | Diagnostics non-blocking load | DONE | Codex | Diagnostics now loads section-by-section with explicit `loading/ready/failed` states while keeping UI responsive. |
| WP-0069 | Startup boot performance + staged initialization | DONE | Codex | Startup no longer blocks on offline bundle apply; background staging + startup status surfaced in shell UI. |
| WP-0070 | "Items" window clarity + rename | DONE | Codex | Renamed Items workspace to Media Library and clarified purpose in UI copy/navigation. |
| WP-0071 | Installer mode clarity + uninstall transparency | DONE | Codex | Added custom NSIS language strings so setup clearly communicates Update/Repair, Full reinstall, and Uninstall; made app-data deletion text explicit (`%APPDATA%`). |
| WP-0072 | NSIS pre-maintenance explainer page | DONE | Codex | Added custom NSIS template page shown before maintenance choice, with one-line explanations for Update/Repair, Full reinstall, and Uninstall (including app-data behavior). |
| WP-0073 | Governance installer mode policy pointers | DONE | Codex | Added concise root-doc policy references so PROJECT_CODEX/AGENTS/MODEL_BEHAVIOR point to canonical installer mode terms and spec source of truth. |
| WP-0074 | Localization output clarity, MP4 defaults, thumbnails, and responsiveness | DONE | Codex | Localization exports now default under `localization/en/<media-stem>/`, yt-dlp defaults prefer MP4, thumbnails self-heal via data URLs, repeated missing-folder prompts were removed, and Diagnostics status work no longer blocks first-open rendering. |
| WP-0075 | Voice-preserving dub audio regression | DONE | Codex | Fixed Kokoro/OpenVoice runtime regressions (current result parsing, mandatory offline-cache warmup, no silent placeholder success) and re-verified on the Queen sample with proof artifacts under `product/desktop/build_target/tool_artifacts/wp_runs/WP-0075/manual_smoke_queen/`. |
| WP-0076 | Reusable voice template library | DONE | Codex | Added app-managed reusable voice templates in Localization Studio with explicit speaker-slot mapping, template-folder reveal/delete, and proof logs under `product/desktop/build_target/tool_artifacts/wp_runs/WP-0076/20260306_132316/`. |
| WP-0077 | Reusable cast packs | DONE | Codex | Added cast-pack persistence plus Localization Studio create/rename/delete/apply flows above reusable templates; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0077/20260306_172806/`. |
| WP-0078 | Multi-reference cloning | DONE | Codex | Extended speaker/template storage and voice-preserving jobs from single-reference clips to backward-compatible multi-reference profiles; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0078/20260306_172806/`. |
| WP-0079 | Auto-match speaker suggestions | DONE | Codex | Localization Studio now pre-fills template/cast-pack mappings using local speaker-key/display-name heuristics and leaves operator review explicit before apply; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0079/20260306_172806/`. |
| WP-0080 | Per-speaker style presets | DONE | Codex | Added reusable style presets across item/template/cast-pack speaker settings and applied soft style-aware delivery hints in TTS preparation; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0080/20260306_172806/`. |
| WP-0081 | Pronunciation locks | DONE | Codex | Added reusable pronunciation override text across item/template/cast-pack speaker settings and applied deterministic replacements in preview plus voice-preserving jobs; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0081/20260306_172806/`. |
| WP-0082 | Emotion and prosody controls | DONE | Codex | Added reusable prosody presets across item/template/cast-pack speaker settings and fed heuristic pacing/terminal-delivery shaping into preview plus voice-preserving jobs; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0082/20260306_172806/`. |
| WP-0083 | Voice QC | DONE | Codex | QC report now includes voice-reference and dubbed-output warnings (silence, clipping, noise, weak similarity) plus Localization Studio rendering; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0083/20260306_204301/`. |
| WP-0084 | Batch dubbing | DONE | Codex | Localization Studio can queue localization batches across selected library items with optional template/cast-pack reuse and follow-on QC/export jobs; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0084/20260306_204301/`. |
| WP-0085 | A/B voice previewing | DONE | Codex | Added A/B variant queueing for one speaker with alternate artifact paths and promote-to-live controls; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0085/20260306_204301/`. |
| WP-0086 | Export stems and alternate versions | DONE | Codex | Speech stems, alternate variant outputs, and alternate export packs are now first-class artifacts and export-pack contents; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0086/20260306_204301/`. |
| WP-0087 | Cross-episode voice memory | DONE | Codex | Added reusable voice-library memory profiles with copied references, suggestions, apply/fork/delete flows, and item speaker linkage; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0087/20260306_204301/`. |
| WP-0088 | Reference cleanup before cloning | DONE | Codex | Added per-speaker cleanup runs (denoise, de-reverb, speech focus, normalize), manifest records, and one-click reuse of cleaned refs; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0088/20260306_204301/`. |
| WP-0089 | Hybrid clone and standard TTS mode | DONE | Codex | Added per-speaker render-mode routing so one item can mix cloned and standard-TTS speakers inside the same dubbing/mix/mux pipeline; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0089/20260306_172806/`. |
| WP-0090 | Subtitle-aware prosody | DONE | Codex | Subtitle-aware pacing is now operator-controlled per item speaker/template/profile and flows through TTS and voice-preserving jobs; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0090/20260306_204301/`. |
| WP-0091 | Character libraries | DONE | Codex | Added reusable character-voice profiles parallel to real-speaker memory, with copied refs, suggestions, apply/fork/delete flows, and Localization Studio controls; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0091/20260306_204301/`. |
| WP-0092 | Variant-aware artifact actions and logs | DONE | Codex | Remediation tranche complete: artifact rerun/status/log handling now honors variant/track/container identity for A/B previews, alternates, QC, and export packs; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0092/20260306_213944/`. |
| WP-0093 | Reference cleanup integrity for multi-reference voices | DONE | Codex | Remediation tranche complete: cleanup is now multi-reference aware, non-destructive by default, and collision-safe per speaker; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0093/20260306_213944/`. |
| WP-0094 | Batch dubbing library-scale selection | DONE | Codex | Remediation tranche complete: batch picker now pages through the full library and no longer truncates selection on item switches; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0094/20260306_213944/`. |
| WP-0095 | Localization Studio manual app smoke | BLOCKED | Codex | Manual smoke remains open and is currently blocked on the post-0.1.6 regression tranche (`WP-0142` to `WP-0148`, with `WP-0143` still in progress), which must land before the next operator validation pass can reasonably close it. |
| WP-0096 | Localization Studio and archiver IA refresh | DONE | Codex | Localization Studio now exposes an in-context ingest block with local import + ASR language selection, while top-level navigation/operator copy consistently use `Video Archiver` and `Instagram Archiver`; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0096/20260307_042415/`. |
| WP-0097 | Shared storage root options and path hydration | DONE | Codex | Added shared-root management in Options plus one frontend hydration store for Library/Localization; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0097/20260307_033244/`. |
| WP-0098 | Legacy downloader library reconciliation | DONE | Codex | Legacy archive reconciliation is now explicitly read-only and NAS-safe: bounded analysis correlates the selected root with the old 4KVDP app-state SQLite, separates managed subscription/playlist containers from unmatched manual folders and loose root files, adds direct SQLite-based import with seeded archive history, and includes the live bounded reconciliation pass against the operator archive; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0098/20260307_042415/`, `product/desktop/build_target/tool_artifacts/wp_runs/WP-0098/20260307_080500/`, and `product/desktop/build_target/tool_artifacts/wp_runs/WP-0098/20260307_195423/`. |
| WP-0099 | Video and Instagram Archiver workflows | DONE | Codex | Video Archiver now exposes YouTube subscription folder-open actions, while Instagram Archiver adds recurring subscription targets with app heartbeat queueing and uncropped recent-media thumbnails; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0099/20260307_042415/`. |
| WP-0100 | Media Library UX and open-path reliability | DONE | Codex | Media Library now uses grouped cards with under-card actions, search/type filters, and shared backend path opening; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0100/20260307_033244/`. |
| WP-0101 | Artifact and output open-action parity | DONE | Codex | Added shared backend shell-path open/reveal commands and rewired major output surfaces through them; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0101/20260307_033244/`. |
| WP-0102 | Startup and performance observability | DONE | Codex | Startup now reports named phases/progress, Diagnostics reads recent local trace rows with process snapshots, and tool lifecycle state is explained as bundled/hydrated/available/optional; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0102/20260307_042415/`. |
| WP-0103 | Window-switch state retention and freeze reduction | DONE | Codex | Top-level pages now stay mounted after first visit so switching windows reuses state instead of remounting/refetching; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0103/20260307_033244/`. |
| WP-0104 | Desktop shell drag and resize ergonomics | DONE | Codex | Dragging is limited to the chrome region and a larger in-surface corner resize handle was added; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0104/20260307_033244/`. |
| WP-0105 | Default media format policy (MP4/JPEG) | DONE | Codex | MP4-first video defaults remain in place and image crawling now prefers practical JPEG/original candidates over less useful alternate encodings where the provider exposes them cleanly; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0105/20260307_042415/`. |
| WP-0106 | Pinterest archive crawler | DONE | Codex | Image Archive now supports Pinterest board/folder batch intake and adds Pinterest-specific full-size/original candidate heuristics in the crawler path; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0106/20260307_042415/`. |
| WP-0107 | Voice cloning research refresh and transfer | DONE | Codex | Added a tracked research corpus under `governance/research/voice_cloning_20260308/` plus spec/roadmap/taskboard sync for the new backend strategy. |
| WP-0108 | Voice backend catalog and recommendation surface | DONE | Codex | Added engine-side backend descriptors plus Diagnostics and Localization Studio recommendation surfaces for managed OpenVoice and experimental OSS candidates; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0108/20260308_014500/`. |
| WP-0109 | Voice benchmark lab and comparison reports | DONE | Codex | Localization Studio now generates durable item-scoped voice benchmark JSON/Markdown reports, ranks current outputs and variants with visible score breakdowns, and exposes them through the artifact browser; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0109/20260308_024800/`. |
| WP-0110 | Experimental BYO voice backend adapters | DONE | Codex | Added local BYO adapter registration, cached probe results, Diagnostics management cards, and backend-catalog status overlays for experimental OSS backends; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0110/20260308_033400/`. |
| WP-0111 | Voice reference curation and bundle promotion | DONE | Codex | Added ranked reference-curation reports, explicit promote-ranked/promote-compact actions for active item speakers, and artifact-browser report discovery; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0111/20260308_141031/`. |
| WP-0112 | Item voice plan and benchmark promotion | DONE | Codex | Added durable item voice plans plus explicit recommendation/benchmark promotion in Localization Studio; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0112/20260308_141931/`. |
| WP-0113 | Experimental backend render runs | DONE | Codex | BYO adapters now support explicit render commands, queued experimental render jobs, backend-aware manifest selection, and request/report artifact discovery in Localization Studio; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0113/20260308_144524/`. |
| WP-0114 | Batch experimental backend runs | DONE | Codex | Added bounded experimental backend batch queueing across selected item sets with backend-aware job summaries and batch IDs; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0114/20260308_161900/`. |
| WP-0115 | Benchmark leaderboard export and compare history | DONE | Codex | Added immutable benchmark snapshot history, leaderboard export artifacts, and Localization Studio compare-history surfaces; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0115/20260308_163900/`. |
| WP-0116 | Backend starter recipes and adapter templates | DONE | Codex | Added typed starter recipes for CosyVoice, Seed-VC, XTTS-style adapters, plus Diagnostics recipe-apply helpers for BYO drafts; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0116/20260308_171300/`. |
| WP-0117 | Promote benchmark winner into template and cast-pack defaults | DONE | Codex | Localization Studio can now promote benchmark winners directly into selected reusable voice templates/cast packs, persist those backend defaults durably, and optionally seed future item voice plans on apply; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0117/20260308_161136/`. |
| WP-0118 | Governance, spec, and traceability audit | DONE | Codex | Stage 1 audit complete: identified contradictory consent-gating language, stale manual-smoke governance scope, stale `WP-0098` proof references, and residual mojibake in governance docs; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0118/20260308_162302/`. |
| WP-0119 | Static code hygiene and architecture audit | DONE | Codex | Stage 2 audit complete: identified monolithic operator/runtime modules, inline Python payload debt, an oversized Tauri command hub, additive schema-migration drift, and stringly-typed artifact identity seams; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0119/20260308_162651/`. |
| WP-0120 | Persistence, data safety, and artifact lifecycle audit | DONE | Codex | Stage 3 audit complete: identified destructive scope drift in `jobs_flush_cache`, non-versioned schema evolution, non-atomic config persistence, indefinite item-derived output growth, and best-effort cleanup/provenance gaps; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0120/20260308_163100/`. |
| WP-0121 | Contention-tolerant performance and responsiveness audit | DONE | Codex | Stage 4 audit complete: identified hidden-page polling, duplicated `jobs_list` intervals, remaining synchronous bridge list commands, thumbnail data-URL amplification, and always-on timers/samplers under heavy external CPU contention; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0121/20260308_163410/`. |
| WP-0122 | Test coverage, proof, and dependency audit | DONE | Codex | Stage 5 audit complete: identified weak installer/offline-hydration coverage, mutable bundled dependency inputs, fragile third-party runtime patching, inconsistent proof rigor, and thin frontend regression coverage; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0122/20260308_163515/`. |
| WP-0123 | Technical debt ledger and remediation program | DONE | Codex | Stage 6 audit complete: consolidated `WP-0118` to `WP-0122` into a governed remediation program and queued `WP-0124` to `WP-0130`; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0123/20260308_163900/`. |
| WP-0124 | Governance hygiene and manual smoke scope reconciliation | DONE | Codex | Governance-only follow-up complete: reconciled roadmap/spec wording drift, expanded `WP-0095` smoke scope to the real current operator surfaces, refreshed stale `WP-0098` proof references, and removed remaining canonical governance mojibake; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0124/20260308_164400/`. |
| WP-0125 | Safe cleanup boundaries and artifact retention policy | DONE | Codex | Cleanup is now previewable and safe by default: terminal job history/logs/job-artifacts/cache are flushed separately from managed and external output-folder deletion, failed deletions remain visible, and Diagnostics now surfaces the working/report/deliverable retention contract; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0125/20260308_183800/`. |
| WP-0126 | Schema versioning and atomic config persistence | DONE | Codex | Persistence hardening is complete: DB migration now uses explicit `PRAGMA user_version` stepping with transactional migration stages, and config/override/adapter-store/secret writes now use shared atomic local persistence helpers; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0126/20260308_191900/`. |
| WP-0127 | Visibility-aware polling and background work suspension | DONE | Codex | Foreground responsiveness is hardened: recurring polling now uses shared visibility-aware non-overlapping loops, hidden mounted pages stop active polling, Jobs refreshes on return, and Localization Studio job-status polling is consolidated into one loop instead of seven parallel `jobs_list` intervals; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0127/20260308_201100/`. |
| WP-0128 | Localization surface decomposition and typed runtime contracts | DONE | Codex | Localization runtime contracts are now typed end to end: item-artifact metadata is serialized from Tauri with explicit kind/job/backend/container/rerun identity, the frontend consumes that through a shared localization runtime module, and `SubtitleEditorPage` no longer reconstructs artifact matching/rerun behavior from filename heuristics inline; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0128/20260308_203600/`. |
| WP-0129 | Reproducible offline bundle and vendor patch hardening | DONE | Codex | Added tracked pinned dependency manifest, opt-in mutable fallback policy, tested Rust vendor patches, and offline payload hash verification; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0129/20260308_222300/`. |
| WP-0130 | Proof standardization and frontend/Tauri regression harness | DONE | Codex | Added repo-wide proof policy, proof-summary template, frontend shared-runtime contract tests, and Tauri artifact/offline-hydration bridge coverage; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0130/20260308_231800/`. |
| WP-0131 | Localization Studio end-to-end audit and repair | DONE | Codex | Localization Studio now auto-prefers translated/en tracks for dub/benchmark flows, exposes a workflow/readiness map to the buried backend/QC/artifact surfaces, and `Mix dub` falls back to source-media audio when separation is unavailable; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0131/20260309_061734/`. |
| WP-0132 | Authenticated archiver sessions and cookie imports | DONE | Codex | Added explicit session inputs for Video URL batch plus YouTube/Instagram subscriptions, JSON/Netscape/file-path cookie normalization, and saved subscription-session sidecars outside SQLite; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0132/20260309_054028/`. |
| WP-0133 | Options-managed per-feature storage roots | DONE | Codex | Added durable per-feature root overrides in Options for Video/Instagram/Image/Localization, kept the old base root as fallback, and removed duplicated root ownership from feature panes; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0133/20260309_055158/`. |
| WP-0134 | Localization outputs library and source/artifact navigation | DONE | Codex | Localization Studio now exposes a grouped `Localization Library` for source media, working artifacts, and deliverables with path actions, and the default localization export root no longer duplicates `localization/en`; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0134/20260309_060801/`. |
| WP-0135 | Diagnostics loading UX and full state snapshot | DONE | Codex | Added shell-visible startup loading details, clearer Diagnostics section progress, and a coherent app-state snapshot/export (JSON + Markdown) for operator + LLM support handoff; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0135/20260309_065500/`. |
| WP-0136 | Media Library list mode and container semantics | DONE | Codex | Media Library now defaults to a full-width archive list, keeps cards as a secondary view, and surfaces provider/container semantics so large subscription, playlist, folder, and loose-file archives are understandable without guesswork; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0136/20260309_093904/`. |
| WP-0137 | Shell move affordance and panel-scroll polish | DONE | Codex | Replaced the ambiguous full-topbar drag model with an explicit `Move window` handle and upgraded dense subscription tables to use panel-local scrolling with pinned action columns; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0137/20260309_164659/`. |
| WP-0138 | Model inventory clarity and required/optional labeling | DONE | Codex | Diagnostics now separates required runtime models from demo/test assets, makes offline-hydrated expectations explicit, and removes the `demo-ja-asr` confusion path; proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0138/20260309_060022/`. |
| WP-0139 | Repo-to-spec crosscheck and governance reconciliation | DONE | Codex | Final crosscheck complete: touched repo surfaces align with current spec/design after reconciling stale `WP-0066` naming drift; remaining open item is the human-run `WP-0095` manual smoke, which stays explicitly queued rather than implied complete. Proof in `product/desktop/build_target/tool_artifacts/wp_runs/WP-0139/20260309_194223/`. |
| WP-0140 | Build output path normalization (no spaces) | DONE | Codex | Canonical desktop build outputs now live under `product/desktop/build_target`, legacy `Build Target` was migrated on disk, scripts/ignore rules were updated, and proof is captured under `product/desktop/build_target/tool_artifacts/wp_runs/WP-0140/20260311_135422/`. |
| WP-0141 | Installer maintenance standard refresh | DONE | Codex | Installer now uses the canonical five-action maintenance standard with explicit keep-vs-full explainer copy, full-action app-data deletion wiring, and verified `0.1.6` desktop target bundles. |
| WP-0142 | Archiver downloader auth and session recovery | IN_PROGRESS | Codex | YouTube recovery slice refreshed bundled yt-dlp to `2026.03.17` and made offline-bundle prep update stale bundled copies; Instagram/session closeout remains split into WP-0191 investigation. |
| WP-0143 | Localization Studio operator flow and output recovery | IN_PROGRESS | Codex | Localization home now renders as a first-screen dashboard with current-item continuation, workflow framing, outputs handoff, and recent localization items instead of a mostly empty ingest stub; pending live operator smoke that the main feature now feels present and usable end-to-end. |
| WP-0144 | Video Archiver surface regression cleanup | IN_PROGRESS | Codex | Video Archiver no longer shares the Media Library import/ASR gate in code; pending operator confirmation that the archive surface is now cleanly separated from Localization Studio. |
| WP-0145 | Localization advanced surfaces discoverability | IN_PROGRESS | Codex | The home surface now explicitly leads into current item, workflow, outputs, and advanced-tool entrypoints through the new dashboard layout; pending operator confirmation that the deeper benchmark/backend/QC/batch surfaces now feel discoverable in normal use. |
| WP-0146 | Window move affordance regression repair | IN_PROGRESS | Codex | The chrome cluster now uses a compact top-right grip with pointer-driven drag, and the shell exposes explicit edge/corner resize hit zones plus a Rust resize-drag fallback; pending operator confirmation on live drag/select/scroll/resize behavior. |
| WP-0147 | Subscription continuity against existing archive folders | BACKLOG | Codex | Reconcile existing per-subscription archive folders and queue only missing media so migrated NAS-backed subscriptions can continue where they left off. |
| WP-0148 | Frameless window bounds and fullscreen correctness | IN_PROGRESS | Codex | The shell now infers full-window mode from the live viewport when native maximize state lags, keeps the visible chassis stretched to the native surface, and removes the oversized move control that was pushing chrome out of place; pending operator side-by-side validation that the invisible blocked native area is gone. |
| WP-0149 | Localization pipeline research and stage contract | DONE | Codex | Research corpus under `governance/research/localization_pipeline_20260312/` now defines the staged cascade as the shipped path and drives the follow-on localization recovery packets. |
| WP-0150 | Installer-state localization runtime and proof hardening | DONE | Codex | Added a staged installer-state-equivalent localization smoke (`wp0150_localization_run_smoke`) and proof bundle under `product/desktop/build_target/tool_artifacts/wp_runs/WP-0150/20260312_023121/`. |
| WP-0151 | Localization runtime failure analysis and reference strategy | DONE | Codex | Added a research-backed runtime diagnosis and reference-acquisition strategy under `governance/research/localization_pipeline_20260312/`, updated spec/design, and queued assisted speaker-reference recovery as the next practical localization step. |
| WP-0152 | Assisted speaker-reference extraction and first-dub recovery | DONE | Codex | Localization Studio now generates source-based speaker-reference bundles after diarization, lets operators apply them into the current voice plan, and is proven through the staged Queen smoke under `product/desktop/build_target/tool_artifacts/wp_runs/WP-0152/20260312_130500/`. |
| WP-0153 | Localization home first-screen contract refresh | DONE | Codex | Spec/design/roadmap now define the home surface as a current-item + next-step + latest-output dashboard with compact shell-status chrome, which grounds the follow-on implementation tranche. |
| WP-0154 | Localization first-screen hierarchy and shell-status demotion | SUPERSEDED | Codex | The `Now / Next / Last Output` orientation layer was retired by operator feedback; remediation continues under WP-0205. |
| WP-0155 | Voice clone basics contract and governance refresh | DONE | Codex | Governance now defines the educational-core reusable-voice contract as capture -> apply later -> dub -> verify clone-vs-fallback truth, and splits the follow-on remediation into narrow packets. |
| WP-0156 | Voice clone truthfulness and fallback disclosure | IN_PROGRESS | Codex | Runtime and operator surfaces are being updated so plain TTS fallback no longer looks like cloned-voice success; explicit clone outcome state is being wired through manifests, reports, and operator-facing benchmark/current-item surfaces. |
| WP-0157 | Reusable voice capture and apply first-run flow | IN_PROGRESS | Codex | First slice landed: Localization Studio now exposes a top-level reusable-voice basics lane for choosing a speaker, capturing refs, saving reusable voice memory, applying saved voice, and continuing the dub without detouring through the advanced asset surfaces. |
| WP-0158 | Reusable voice asset contract and drift guardrails | BACKLOG | Codex | Templates, cast packs, memory, and character profiles need tighter shared contract and regression coverage so reusable-voice behavior does not drift apart behind the scenes. |
| WP-0159 | Voice clone basics installer-state smoke and proof | BACKLOG | Codex | The educational-core reusable-voice promise needs a real installer-state proof path across multiple items, including explicit evidence of clone success versus fallback. |
| WP-0160 | Legacy YouTube subscription continuity state decoupling | IN_PROGRESS | Codex | App-managed continuity state landed, but operator smoke still shows mixed legacy-override vs new managed-root behavior is hard to reason about when NAS-backed imports and newer subscriptions coexist. |
| WP-0161 | Video Archiver progress UX vs 4K Downloader | REVIEW | Codex | Phase 1 done: Type/Downloaded/Status columns added. Detected count deferred. |
| WP-0162 | Global Authentication and Sessions | DONE | Codex | Global YouTube auth config in Options; engine jobs/subscriptions fall back to global cookies when no per-job or per-subscription cookie exists; verified via `cargo check` + `npm run build`. |
| WP-0163 | Built-in Visual Debugger for Agents | DONE | Codex | `html2canvas` frontend capture + `admin_save_snapshot` Tauri command + hotkey (Ctrl+Shift+S) + programmatic trigger (`window.__voxVulgiRequestSnapshot()`); documented in `AGENTS.md`. |
| WP-0164 | Localization Studio Declutter | REVIEW | Codex | Workflow Map buttons grouped into 4 categories (Captions, Voice, Quality, Advanced) with grid layout. |
| WP-0165 | Archive Pages Progressive Disclosure | REVIEW | Codex | Quick/Advanced toggle shared across Video, Instagram, Image archive pages; Quick hides subscriptions, presets, groups, legacy import, Pinterest. |
| WP-0166 | Terminology and Help Text Cleanup | REVIEW | Codex | Renamed: ASR lang→Source language, dub truth→Clone status, Phase 2 packs→Voice cloning packages, Flush→Clean up, hydration→initialization, refs→voice samples, voice memory→saved voice, reconciliation→import, Tool lifecycle→Component status. |
| WP-0167 | Diagnostics Dashboard Summary | REVIEW | Codex | 5 clickable status tiles at top: App version, Voice packages, FFmpeg, Storage, Recent failures. Each scrolls to detail section. |
| WP-0168 | Jobs Page Cleanup | REVIEW | Codex | Test job behind Developer tools toggle, secondary actions in More… dropdown, tree indentation, paused state explanation. |
| WP-0169 | Options Page Consolidation | REVIEW | Codex | 4 feature-root cards replaced with single table (Feature/Path/Status/Actions), auth UX improved with help text and clear button. |
| WP-0170 | Media Library Search and Filters | REVIEW | Codex | Added Source filter (YouTube/Instagram/Local) and Sort by (Date/Title) to existing search+type+view+group controls. |
| WP-0171 | Headless Agent Bridge | REVIEW | Codex | Localhost HTTP server with /agent/navigate, /agent/snapshot, /agent/state, /agent/health; port file discovery; frontend event listeners; AGENTS.md documented. |
| WP-0172 | Localization Studio Built-in Manual | REVIEW | Codex | In-context (?) help on 18 section headers with What/When/Steps/Concepts; "Show all help" toggle for learning mode. |
| WP-0173 | Localization Studio Keyboard Shortcuts | REVIEW | Codex | Ctrl+Enter run, Ctrl+Shift+E export, Ctrl+Shift+R refresh, Ctrl+1..5 section jumps; visible reference in Workflow Map. |
| WP-0174 | Batch-on-Import Discoverability | REVIEW | Codex | Auto-processing toggles surfaced in Localization Studio Import card with immediate save; Source language label improved. |
| WP-0175 | Subtitle Editing Undo/Redo | REVIEW | Codex | Ctrl+Z/Ctrl+Shift+Z undo/redo for subtitle text, timing, and speaker changes with 50-op stack; resets on track switch. |
| WP-0176 | Drag-and-Drop Import | REVIEW | Codex | Drop media files onto Localization Studio home; visual overlay indicator; multi-file batch; format validation. |
| WP-0177 | Glossary and Custom Term Mapping | REVIEW | Codex | Engine already had glossary apply; added Tauri commands + full CRUD UI with add/remove/export CSV/import CSV; help panel. |
| WP-0178 | Sticky Quick-Actions Bar | REVIEW | Codex | Sticky bottom bar with item title, status, Run/Export/Open Outputs buttons; visible at all scroll positions. |
| WP-0179 | Translation Style Settings | REVIEW | Codex | Style selector (Neutral/Formal/Informal) + honorific handling (keep/translate/drop) in Track card; persists in localStorage. |
| WP-0180 | Bilingual Subtitle View | DONE | Codex | Already implemented: bilingual checkbox + auto-pair/manual track selection + paired text column in segment table. |
| WP-0181 | Pipeline Presets | REVIEW | Codex | 4 built-in presets (Japanese Anime, Korean Variety, Quick Subtitles, Full Dub); one-click configures ASR lang + batch-on-import rules. |
| WP-0182 | Per-Segment Audio Preview | REVIEW | Codex | Play/stop button on each segment row; seeks mix dub WAV to segment start, auto-stops at end; visual indicator on playing segment. |
| WP-0183 | CosyVoice 2 Backend Integration | REVIEW | Codex | Repo cloned, render wrapper written (cross_lingual inference), adapter template registered. Needs model weights download + PyTorch deps. |
| WP-0184 | Fish Speech 1.5 Backend Integration | REVIEW | Codex | Repo cloned, render wrapper written (API server client), adapter template registered. Needs model weights + API server running. |
| WP-0185 | Clone Outcome Notification | REVIEW | Codex | Auto-notification when clone outcome changes: success=notice, fallback=error with segment counts and guidance. |
| WP-0186 | Per-Segment Clone Fallback Breakdown | REVIEW | Codex | Tauri command loads manifest per-segment data; colored dot per segment (green=cloned, red=fallback, grey=standard); tooltip with error detail. |
| WP-0187 | Clone Pre-Flight Check | REVIEW | Codex | Validates speaker profiles before voice-preserving job; warns about missing voice samples with confirm dialog. |
| WP-0188 | Reference Quality Score Explanation | REVIEW | Codex | Mini quality breakdown (duration/clips/warnings) on voice basics card + reference tips for empty state. |
| WP-0189 | Offline bundle voice-pack compatibility repair | DONE | Codex | Fixed the offline bundle voice-pack pin conflict, captured proof under `product/desktop/build_target/tool_artifacts/wp_runs/WP-0189/20260419_193510/`, and shipped desktop build `0.1.8`. |
| WP-0190 | App Readability and Visible Version | REVIEW | Codex | Raised desktop UI font scale and added current app version next to the top-bar VoxVulgi brand; automated build proof captured, manual UI smoke still required before DONE. |
| WP-0191 | Instagram Archiver Failure Investigation | IN_PROGRESS | Codex | Operator smoke reproduced profile-expansion failures (`Unable to extract data`) with failed one-shot jobs that never create item/output context; implementation follow-up now starts from those traces and logs. |
| WP-0192 | Localization Studio large-library load containment and first-open stabilization | IN_PROGRESS | Codex | Bound the first-open load path so Localization Studio stays usable against six-figure libraries instead of trying to hydrate the full library and every advanced surface up front. |
| WP-0193 | Jobs operator context and direct output navigation | IN_PROGRESS | Codex | Make Jobs rows identify what work they correspond to, especially for failed archiver runs with no item row yet, and provide direct navigation to logs, artifacts, outputs, and source context. |
| WP-0194 | Diagnostics truthfulness and bounded storage scans | IN_PROGRESS | Codex | Fix Diagnostics sections that remain loading or report stale tool state after startup, and keep storage accounting bounded on large/NAS-backed roots so the dashboard settles truthfully. |
| WP-0195 | Archive output naming and target-root clarity | IN_PROGRESS | Codex | Make archive folder/file naming and target-root messaging predictable across imported 4KVDP NAS targets, new VoxVulgi subscriptions, one-shot YouTube downloads, and Instagram archive runs. |
| WP-0196 | Operator-configurable desktop font scale | IN_PROGRESS | Codex | Add a real in-app desktop font-size preference instead of relying only on the current hard-coded global scale increase. |
| WP-0197 | Localization operator workspace decoupling | REVIEW | Codex | Workspace-only intake landed: Localization home/editor now read from an explicit localization workspace, and archive/media pages no longer open shared items straight into Localization; pending operator smoke. |
| WP-0198 | Localization import handoff and workspace visibility recovery | REVIEW | Codex | Fix the Localization-only import path so operator imports actually enter the localization workspace, appear in Current Item/Localization Library, and preserve an obvious handoff after import rather than disappearing into Jobs alone. |
| WP-0199 | Localization explicit start contract and run-progress surface | REVIEW | Codex | Remove the confusing implicit auto-start feel from Localization intake, make pre-start settings obvious before work begins, and expose stage-level run/progress/failure truth from the main Localization surface instead of relying on Jobs as the only source of truth. |
| WP-0200 | Diagnostics startup truth and operator-ready component state | REVIEW | Codex | Stop Diagnostics from behaving like a cold second startup after the app is already hydrated, and keep component readiness/progress/failure state truthful and visible without forcing operators to infer status from silent reloads. |
| WP-0201 | Localization empty-transcript fail-fast and stage truth | REVIEW | Codex | ASR/translation now fail before persisting empty tracks, stale empty source/translated tracks block downstream continuation, and automated proof is captured; pending live Queen-sample/operator smoke before DONE. |
| WP-0202 | Diarization pack dependency integrity and self-repair | REVIEW | Codex | Pins, runtime validation, Diagnostics repair state, live venv repair, and Queen-media diarization probe completed; proof under `product/desktop/build_target/tool_artifacts/wp_runs/WP-0202/2026-04-25_0315_wp0202_0204/`. |
| WP-0203 | Localization import cancellation and duplicate queue containment | REVIEW | Codex | Same-path active import reuse, existing-item reuse, batch cancel propagation, and Queen-sample duplicate containment smoke completed; proof under `product/desktop/build_target/tool_artifacts/wp_runs/WP-0203/2026-04-25_0315_wp0202_0204/`. |
| WP-0204 | Localization terminal truth and deliverable visibility | REVIEW | Codex | Shared terminal outcome contract now feeds Localization home/library and Jobs; Queen item shows failed-before-deliverable truth; proof under `product/desktop/build_target/tool_artifacts/wp_runs/WP-0204/2026-04-25_0315_wp0202_0204/`. |
| WP-0205 | Localization home dashboard simplification | IN_PROGRESS | Codex | Retire the `Now / Next / Last Output` orientation grid (supersedes WP-0154), trim home polling cost. |
| WP-0206 | Clear failed localization runs per item | IN_PROGRESS | Codex | Per-item engine + UI action to clear failed run history, default-non-destructive with optional orphan-artifact purge. |
| WP-0207 | Localization workflow panel as the primary surface | IN_PROGRESS | Codex | Merge `loc-workflow` jump grid + `loc-run` card into one stepper-style Workflow Panel with run controls + per-stage rows + advanced expander; inline stage controls deferred to WP-0208+. |
| WP-0208 | Localization workflow inline stage controls | IN_PROGRESS | Codex | Pull primary Run button + minimal option pickers into each Workflow Panel stage row so the operator can drive the full pipeline without scrolling to `loc-track`. |
| WP-0209 | Visual debugger state dump and console buffer | IN_PROGRESS | Codex | Pair the existing PNG snapshot with a JSON state-dump (page, editor item, viewport, scroll, filtered localStorage, mounted loc-* ids, last 200 console entries); add `/agent/dump` bridge endpoint and `window.__voxVulgiRequestDump`. |
| WP-0210 | Bridge and visual debugger reliability | IN_PROGRESS | Codex | Stop the StrictMode async-listener race that doubled every snapshot/dump capture; add an `agent_bridge.json` PID/start-time sidecar and clean both files up on Tauri exit so a stale port file no longer silently misleads agents. |
| WP-0211 | Localization editor master-detail layout | IN_PROGRESS | Codex | Replace the 13-card editor stack with one panel: item header strip + left rail of 8 stages + right pane that renders only the selected stage. Strip card chrome, delete First Dub Guide / Reusable Tools / Advanced Tools redundancy. Per operator: "single panel, not a fan of the card system." |
| WP-0212 | Safe Mode pill placement and exit-rehydrate notice | IN_PROGRESS | Codex | Replace the always-visible top-bar "Recovery" pill with a smaller end-of-topbar Safe Mode toggle ("Safe Mode ON" / "Safe Mode OFF") and surface an in-app dismissable notice on exit warning that a restart is required to rehydrate bundled assets skipped during Safe Mode startup. |
| WP-0213 | Build rules: headless verification and no cards | REVIEW | Codex | Created `build_rules.md`, linked it from `PROJECT_CODEX.md`, and pointed agent notes at the headless visual/app-boundary verification rule plus the no-new-cards UI rule. |
| WP-0214 | Localization setup-first workbench and export bundle | IN_PROGRESS | Codex | Setup-first home, source-copy export, language-marked filenames, successful-output rows, and subtitle-only run mode are implemented; build/checks pass, pending visual smoke after the running WebView reloads the rebuilt frontend. |
| WP-0215 | Localization multi-speaker diarization controls | REVIEW | Codex | Speaker-count controls now flow through setup-first runs, editor runs, direct diarization, baseline/pyannote execution, and diarization reports; build/checks and headless snapshot proof captured. |
| WP-0216 | Localization automatic voice-reference continuation | REVIEW | Codex | Auto source-reference generation/apply now runs before voice-plan pause and queues dub when references are ready; live item repaired to the next blocker, missing voice-preserving TTS pack. |
| WP-0217 | Diagnostics voice-pack installer stale-state truth | REVIEW | Codex | Phase 2 install state is now normalized against the job DB so interrupted installs do not keep showing `Installing...`; successful `done` steps also count as complete. |
| WP-0218 | Localization voice setup as start-flow UX | REVIEW | Codex | English dub runs now queue one-time voice-cloning setup from the localization orchestrator and resume automatically after setup; Start surfaces plain-language voice setup status instead of sending operators to Diagnostics. |
| WP-0219 | Build script offline payload reuse | REVIEW | Codex | Governed desktop builds now validate/reuse the existing offline payload by default, refresh only when missing/stale or explicitly requested, and expose validate/refresh/force-refresh npm aliases. |
| WP-0220 | Multi-library video archive and archiver IA | REVIEW | Codex | Multi-library registry/API, active library selector, subscription library binding, old 4K-style `library_root/<folder>` paths, media-list sorting, Options advanced recovery move, Video Archiver tab split, and explicit browser-cookie source selection are implemented. 2026-06-04 follow-up fixed the persisted built-in `{provider}/{channel}` preset that still created `<root>/youtube/<channel>` for new singles, changed default preset UI to `{channel}`, and added an existing-folder merge prompt with archive seeding for new subscriptions/playlists. UI snapshot proof still pending without running bridge. |
| WP-0221 | UI freeze diagnostic instrumentation | REVIEW | Claude | Worker-driven main-thread heartbeat + OS-thread skew detector + per-command timing for the 8 most-suspect Tauri commands + new `POST /agent/freeze_event` agent-bridge route + `agent_bridge_port` Tauri command, recorded into `diagnostics_trace.jsonl` and surfaced via a "Freeze events" subsection inside the existing Diagnostics trace card (no new card). v0.1.19 follow-up adds `POST /agent/freeze_dump`, `agent_freeze_dump_now` Tauri command, `vvfreeze.cmd` one-click trigger, and the canonical freeze-report path. v0.1.20 adds Worker liveness heartbeat (`worker_alive` every 30 s) and lowers the freeze threshold from 500 ms to 250 ms after early v0.1.19 traces showed zero `freeze_detected` rows over 11.5 h — needed to disambiguate "Worker dead" from "freeze is at the WebView UI/compositor layer". `CLAUDE.md` and `AGENTS.md` document the canonical paths and worker-alive sanity check. Built v0.1.20 (`product/desktop/build_target/Current/release/bundle/nsis/VoxVulgi_0.1.20_x64-setup.exe`); app-boundary visual proof pending. Observation-only WP. |
| WP-0222 | Reveal media file exit code truthfulness | IN_PROGRESS | Claude | `explorer.exe /select,<file>` returns exit code 1 even on success; `shell_reveal_target` now treats exit 1 as success only on the `/select,` path. Folder-only reveals keep strict success semantics. Ships in v0.1.19. |
| WP-0223 | SQLite contention and N+1 list queries | IN_PROGRESS | Claude | `db::open` now sets `PRAGMA synchronous=NORMAL` (safe with WAL, eliminates per-write fsync cliff). `list_youtube_subscriptions` rewritten as a single SELECT with `GROUP_CONCAT` subquery for group_ids (was 1 main + N hydrate queries). Target: list commands drop from 3-5 s under load to sub-100 ms. Shipped in v0.1.22. v0.1.22 freeze trace showed partial improvement (`youtube_subscriptions_list` 3462 → 2042 ms) but `library_list` still hit 13829 ms — root cause was writer holding the lock, not the queries themselves. Structural fix continues in WP-0224. |
| WP-0224 | Agent bridge CORS and read-only UI DB connections | REVIEW | Claude | Two related fixes shipped in v0.1.23. (A) CORS: `handle_agent_request` now returns `Access-Control-Allow-Origin: *` headers on every response and `204 No Content` for OPTIONS preflight, so the freeze-detector Worker can post to `/agent/freeze_event`. (B) Read-only UI connections: new `db::open_readonly` (SQLITE_OPEN_READ_ONLY); rewired five UI list queries to bypass the job-runner write queue. v0.1.23 freeze trace confirmed both fixes worked (20 `worker_alive` rows; `youtube_subscriptions_list` 3462 ms → 7-15 ms; `library_list` 13829 ms → 4 ms after first call). Read-only sweep continues in WP-0226. |
| WP-0226 | Comprehensive read-only UI read sweep | IN_PROGRESS | Claude | Extends WP-0224 read-only pattern to every panel-mount engine read so no panel switch can stall on writer lock. Ten engine functions rewired to `db::open_readonly`: jobs::list_jobs, jobs::get_job, jobs::list_jobs_for_item, jobs::get_queue_control, jobs::get_runtime_settings, library::get_item_by_id, subtitle_tracks::list_tracks, subtitle_tracks::get_track (and load_document via get_track), video_libraries::get_video_library_by_id. Plus InvokeTimer added to five previously-uninstrumented Tauri commands: library_get, jobs_list, jobs_list_for_item, jobs_queue_control_get, jobs_runtime_settings_get. Ships in v0.1.24. Writer instrumentation explicitly deferred (would be diagnostic only; the read sweep removes the user-visible symptom). |
| WP-0227 | Voice pack auto-install and resume | SUPERSEDED | Claude | Shipped in v0.1.25 (auto-enqueue + resume). The auto-enqueue half was a net regression — operator reported the app became unusable while the install ran in background. Auto-enqueue removed in WP-0228; resume logic kept. Direction superseded by WP-0228. |
| WP-0228 | Voice pack auto-install rollback (v0.1.26) | IN_PROGRESS | Claude | Emergency rollback after operator reported v0.1.25 made the app unusable ("freezes all the time, have not touched it once because nothing happens because of freeze"). Removes the WP-0227 startup auto-enqueue thread in `lib.rs`; voice-pack install returns to being explicitly operator-triggered from Diagnostics. Keeps the resume logic in the install job handler so when the operator does click Install, it carries forward previously-`done` steps instead of restarting. Ships in v0.1.26. |
| WP-0229 | Phase2 install short-circuit when packs already present | BACKLOG | - | Found while answering operator question "do I have to install on every startup". Each `install_*_pack` function in `tools.rs` runs all pip commands unconditionally even when the pack is already installed — pip's "Requirement already satisfied" walk still costs tens of seconds per pack, plus Kokoro warmup re-loads voice models from disk every time. WP adds `install_*_pack_if_needed` wrapper for each of the 5 packs that short-circuits when `*_pack_status(paths).installed` is true; Phase2 install job handler calls the wrapper unless a new `force: bool` parameter is set; Diagnostics page gets a "Force reinstall all packs" button for repair. Implementation Plan in the WP file is detailed enough for a no-context model. Ready for pickup. |
| WP-0230 | Phase2 install progress bar and status truthfulness | IN_PROGRESS | Claude | Operator: "i want a progress bar for all voicepacks when installing i am just watching the same 'Live progress: interrupted' badge, not really comforting". Pure frontend (DiagnosticsPage.tsx) change — replace the one-word badge with a `<progress>` bar, current-step strip with elapsed time, per-pack icons, and an auto-poll every 2 s while a job is active. Headline text resolves to one of five honest states (not yet installed / queued / installing step X of M / interrupted N of M complete / all M done) instead of just "interrupted". **2026-05-18 scope extension**: also surface real bytes/sec/ETA by parsing pip stdout + hooking HF Hub `tqdm` callbacks; same progress feed consumed by the WP-0235 first-run flow. See WP file "Scope Extension". |
| WP-0231 | Neural TTS pack hf_hub/transformers pin and recovery | IN_PROGRESS | Claude | Operator-reported failure 2026-05-18: Kokoro warmup fails with `ImportError: huggingface-hub>=1.5.0,<2.0 is required … but found huggingface-hub==1.4.1`, cascading to OpenVoice. Root cause: `tts_neural_local_v1` group in `pinned_dependency_manifest.json` does not pin `transformers` or `huggingface_hub`, so resolver drift + a leftover hf_hub 1.4.1 from a pre-WP-0189 install break the import-time version check. Fix: pin `transformers==5.8.1` + `huggingface_hub==1.5.0` in the Kokoro group, switch its `pip install` to `--upgrade`, and add a one-shot `--force-reinstall --no-deps` recovery on warmup failure. No new installer build; release is gated on operator request. |
| WP-0232 | Per-pack hashed lockfile and `--require-hashes` installs | IN_PROGRESS | Claude | Tier-1 hardening, successor to WP-0231. Replaces "pip install <list of pins>" with a per-pack hashed lockfile generated at build time via `pip install --report` and consumed via `pip install --require-hashes --no-deps -r <lockfile>`. Eliminates resolver gambling at user install time — WP-0231's class of bug stops existing. New script `governance/scripts/lock_python_packs.ps1`, lockfiles committed under `product/engine/resources/tooling/lockfiles/`, manifest schema gains a per-pack lockfile path. Prerequisite for WP-0236, WP-0237. |
| WP-0233 | CI / pre-build warmup gate for every Python pack | IN_PROGRESS | Claude | Tier-1 hardening. New `governance/scripts/pack_warmup_gate.ps1` runs each `install_*_pack` + warmup probe in a throwaway venv before `build_desktop_target.ps1` can produce an installer. Catches resolver drift on the developer side instead of the user's machine. Release blocker; documented in `build_rules.md`. CI workflow added if a runner is available. Reuses `voxvulgi_offline_bundle_prep` as the execution engine. |
| WP-0234 | Atomic per-pack install with staging and rollback | IN_PROGRESS | Claude | Tier-1 hardening. **2026-05-18 scope decision**: operator picked the smallest implementation (install-state journaling + auto force-reinstall on detected bad state) over the full staging or per-pack venv split. Journals `{lockfile_sha, started_at_ms, finished_at_ms, last_outcome}` per pack; on next install, if prior state is `in_progress` (crashed) or `failed`, promotes `--upgrade` to `--force-reinstall` so broken state is overwritten instead of layered on. Full staging / per-pack-venv split remain valuable but are deferred until field evidence shows they are needed. |
| WP-0235 | First-run "Set up voice packs" flow (outside Diagnostics) | IN_PROGRESS | Codex | 2026-05-21: Localization entry now reads voice-pack readiness directly, surfaces setup/repair actions, and blocks English dub starts while packs need install/repair. Full runtime proof now passes on Haerin single-speaker exact-1 and Queen/Miyeon multi-speaker range-2..4 samples with dub/mix/mux deliverables under `product/desktop/build_target/tool_artifacts/wp_runs/WP-0235/`. Remaining before DONE: installed-app bridge snapshot/dump, setup-flow UI proof, and progress/repair polish surfaced by runtime evidence. |
| WP-0236 | One-click "Repair this pack" using lockfile | BACKLOG | - | Tier-2 user-friendliness. Per-pack Repair button on both Diagnostics and the WP-0235 first-run flow. Wipes the pack's installed files and reinstalls from lockfile + bundled wheels. Per-pack only; "Repair all" intentionally not added. Surfaces a plain-language result message. Logs to diagnostics trace as `pack_repair_*` so freeze tooling (WP-0221) picks it up. Depends on WP-0232 (lockfile) + WP-0234 (atomic install). |
| WP-0237 | Bundle pre-resolved wheels in installer (offline-default) | BACKLOG | - | Tier-3 hardening. Build-time payload prep bundles every wheel listed in the WP-0232 lockfiles into the installer. First-run pip install runs `--no-index --find-links <bundled> --require-hashes` — PyPI not required. Installer size +~1.5 GB; install time drops from 5–15 min to <60s. Removes the entire network-during-install failure surface. Depends on WP-0232. |
| WP-0238 | Optional bundle of model weights in the installer | BACKLOG | - | Tier-3 hardening. Optionally ships Kokoro voices + OpenVoice V2 checkpoint + Spleeter 2stems model inside an installer variant ("full offline"). With WP-0237 + WP-0238, the entire install is network-free. License audit is the gating risk (OpenVoice / Kokoro / Spleeter licenses must allow redistribution; documented in installer license page). Default vs. opt-in installer variant decided post-size-measurement. |
| WP-0239 | Operator verification of voice-pack reliability build (v0.1.27) | IN_PROGRESS | Operator | Test checklist covering WP-0231 + WP-0232 + WP-0233 + WP-0234 + WP-0230 against the v0.1.27 desktop build that bundles them. Sections A (gate enforcement), B (lockfile + hf_hub), C (install state journal + crash recovery), D (progress UI truthfulness), E (end-to-end voice clone). Spleeter failure in A.2 is expected and tracked separately. Acts as the gate to flip the upstream WPs from IN_PROGRESS to DONE. |
| WP-0240 | vvstart repo-rule acknowledgement contract | DONE | Codex | Added required acknowledgement contract to `vvstart.cmd`, `just vv-start`, and `governance/scripts/vv_start.ps1`; proof bundle under `product/desktop/build_target/tool_artifacts/wp_runs/WP-0240/2026-05-20_vvstart_ack/`. |
| WP-0241 | Emergency freeze containment for hidden Diagnostics probes | DONE | Codex | Shipped and silently installed v0.1.28. App no longer pre-mounts hidden Diagnostics after startup; Diagnostics initial refresh now returns while hidden. Proof: contracts/build passed, v0.1.28 bridge on Options, `heavy_probe_descendant_count=0`, proof bundle under `product/desktop/build_target/tool_artifacts/wp_runs/WP-0241/2026-05-20_hidden_diagnostics_probe_containment/`. |
| WP-0242 | Sibling external freeze watchdog | DONE | Codex | Added `vvwatch.cmd` + `governance/scripts/vv_watch.ps1`, an out-of-process monitor that records Windows process responsiveness, process tree/heavy children, bridge health/state, DB lock probes, NAS root probe, Python package state, and latest freeze-report slow commands while the app is degraded. Self-test passed; live run wrote `diagnostics/external_watch/watch_20260520-215114` and found app/bridge responsive, no heavy children, NAS path responsive, but current freeze report still dominated by `instagram_subscriptions_queue_all_active`, `library_get`, and `jobs_queue_control_get`. |
| WP-0243 | DB command contention containment | DONE | Codex | Shipped and silently installed v0.1.29. Gated the global Instagram heartbeat to Instagram Archive, reduced its cadence, split Jobs active polling into a lightweight job snapshot, bounded Jobs context hydration, shortened read-only SQLite busy waits, and added explicit `database_locked` / `database_busy` trace events. Proof bundle: `product/desktop/build_target/tool_artifacts/wp_runs/WP-0243/2026-05-20_db_command_contention_containment/`. Fresh v0.1.29 trace has 0 current-pid `instagram_subscriptions_queue_all_active` rows on Video Archiver, but still shows `youtube_subscriptions_list` database locking, which is carried forward. |
| WP-0244 | DB startup read/write contention | DONE | Codex | Installed v0.1.34. Video Archiver startup/list reads are read-only where applicable, archive stats no longer opens SQLite, visible list reads run via async `spawn_blocking`, and fresh freeze report showed 40-144 ms command timings with no DB lock/busy/slow/freeze rows. Proof: `product/desktop/build_target/tool_artifacts/wp_runs/WP-0244/2026-05-21_db_startup_read_write_contention/summary.md`. |
| WP-0245 | Jobs/Library batched queries and install-command tracing | IN_PROGRESS | Claude | Operator goal 2026-05-22 ("make my app work"). v0.1.50 freeze trace contained 1,071 `command_slow` rows dominated by 672× `library_get` and 227× `item_outputs`, plus zero install-related rows. JobsPage now hydrates job/item context through two new batched commands (`library_get_many`, `item_outputs_many`); pack-install Tauri commands gain `InvokeTimer` and the four sync ones become `async` + `spawn_blocking`. Engine + Tauri `cargo test` + 50 desktop contract tests green. Operator-relayed install + new-trace proof bundle pending. |
| WP-0246 | Localization Studio Spleeter deadlock fix, job-spawn timeout/cancel hardening, stuck-job watchdog | IN_PROGRESS | Claude | Operator 2026-05-29: Localization Studio "never worked", a few-second clip stuck 5+ h. Root cause (live process tree + job DB): `separate_audio_spleeter` deadlocked because the embedded script used `Separator("spleeter:2stems")` whose default `multiprocess=True` spawns an `os.cpu_count()` `Pool()` — ~33 workers each re-import TensorFlow under Windows spawn, then deadlock (job pinned at `progress=0.25`, idle workers + blocked ffmpeg pipe). Fix: `multiprocess=False`. Hardened all 7 Python-model/ffmpeg spawn sites (spleeter, demucs, pyannote+fallback diarize, pyttsx3, neural TTS, voice-preserving TTS, vocal-cleanup) onto `run_command_output_with_control` (timeouts 1800/3600 s, cancellable). Added `run_job_stall_watchdog` on the runner heartbeat: WARN at 600 s no-progress, auto-fail backstop at 7200 s (above the longest command timeout so healthy jobs are never killed). Shipped in desktop build 0.1.54. Operator install + verification on the test clip pending. |
| WP-0247 | Archiver session refresh and job context recovery | REVIEW | Codex | Implemented execution-time Options-global YouTube auth precedence for old jobs, removed duplicate YouTube cookie/session UI from YouTube archiver panes, fixed direct-job source context/collapsed batch summaries, added YouTube single downloaded history with fuzzy search/order over all YouTube video candidates, and added Media Library `Single videos / legacy` filter. Focused tests pass; managed desktop target `0.1.55` built with bridge snapshots/dumps. After confirming no active jobs, the old installed-app process was closed and the fixed `0.1.55` release executable is now the live app process; persistent shortcut/start-menu update still requires elevated installer approval. Proof: `product/desktop/build_target/tool_artifacts/wp_runs/WP-0247/2026-06-01_archiver_session_job_context/summary.md`. |
| WP-0248 | Jobs trustworthy retry, recovery, and inspection surface | REVIEW | Codex | Expanded 2026-06-03 from old-batch retry/delete/title fixes into full Jobs/Queue recovery truth for failed, retried, bundled, and batched YouTube downloads. Delivered persisted retry lineage, canonical batch health/detail, Job Detail / Attempt History, filters/fuzzy search/export/backfill/repair, retry dry-run/auth gating, large-batch inspection, first-class Cookie Editor `cookie.js` auth input, updated default preflight URL, stale DB-lock banner recovery, and one bounded transient-lock Jobs refresh retry. Follow-up build `0.1.66` fixes old-batch display/retry truth: batch rows lead with canonical video targets, separate historical attempt counts, and retry only unresolved canonical targets. Live `42a89117-db6e-4202-970a-fe62a01b2dbe` evidence: `55` videos, `55` downloaded with linked library items and existing media files, `0` unresolved, with `96` failed and `23` canceled rows retained as historical attempts. Proof: `product/desktop/build_target/tool_artifacts/wp_runs/WP-0248/2026-06-04_batch_target_health_status/summary.md`. |
| WP-0249 | Video library export/import and offline recovery | REVIEW | Codex | Metadata-only video-library bundle export/import, NAS-offline create/select-library prompt before YouTube queueing, reconnectable old NAS rows, item metadata copy/move, and subscription move/rebind are implemented. Subscription copy remains deferred because source URLs are unique. Proof: `governance/snapshots/WP-0249/video_library_controls_live3_1780538584350.png`. |
| WP-0250 | WebView2 occlusion/background renderer-freeze fix | IN_PROGRESS | Claude | Operator 2026-06-10: app freezes while idle in background. Live bridge dump (v0.1.66, pid 135748, Responding=False, backend alive) proved a distinct freeze class from WP-0242/0243/0244: zero `command_slow`/`freeze_detected`, native `runtime_sample` ticking at 0% CPU, but `main_thread_alive` AND `worker_alive` (separate renderer threads) both died together ~2h22m after a `blur` — the whole WebView2 renderer was suspended by Chromium native window occlusion. Fix: set `additionalBrowserArgs` to `--disable-features=...CalculateNativeWinOcclusion` + `--disable-backgrounding-occluded-windows`/`--disable-renderer-backgrounding`/`--disable-background-timer-throttling` (wry defaults re-included), config-contract test, version bump. Off branch `fix/webview2-occlusion-freeze`. |
| WP-0251 | Localization dub Kokoro offline-cache honest readiness | IN_PROGRESS | Claude | Operator 2026-06-15: Localization Studio "never worked, not even close to producing an audio file or subs" (Haerin KO single-speaker clip). Root cause (canonical job DB row `5b648db6...`, status `failed`): `dub_voice_preserving_v1` dies at the first Kokoro synth — `KPipeline` `hf_hub_download('hexgrad/Kokoro-82M')` resolves from the app-local HF cache (`HF_HOME=<appdata>/cache/huggingface`, `HF_HUB_OFFLINE=1`) which was EMPTY; Kokoro only ever landed in the user `.cache`, and a stale `.warmup_ok` marker made the readiness gate report "installed" so re-provisioning was skipped → `OfflineModeIsEnabled`/`LocalEntryNotFoundError`, exit 1, no audio/subs, follow-up separation/mix/mux/subtitles never ran. Fix (`tools.rs`): `kokoro_app_cache_ready` mirrors the offline resolver (refs/main→sha→config+pth+af_heart voice) and now gates both Kokoro pack statuses; warmup writer refuses a dishonest marker; regression unit test added; `cargo check`+test green. Render-level proof: exact Haerin request now yields `clone_preserved` cloned segment. Live app cache populated so operator can retry now. Quality upgrades split to WP-0252. Build gated on WP-0252. |
| WP-0252 | Localization quality stack: whisper large-v3 ASR + CosyVoice 2 voice clone | IN_PROGRESS | Claude | Operator 2026-06-15 chose "full quality stack now". Field research (workflows) found 2 quality-fatal defects beyond WP-0251: ASR hardcoded to whisper-tiny (garbled Korean) and Kokoro+OpenVoice timbre-paint being weak. DONE+compiling: ASR→large-v3 q5_0 default (manifest sha re-verified, `config/asr_model_id.txt` swappable, 6 enqueue sites + health gate + bins). CosyVoice 2 in an isolated torch-2.3.1 venv (`tools/python/venv_cosyvoice`) + `CosyVoice2-0.5B` model (4.86GB) + Matcha-TTS; `tools.rs` honest `cosyvoice_pack_status`, `jobs.rs` `run_cosyvoice_dub_render` + dub-job routing reusing the separation→mix→mux→subtitle follow-up, `default_dub_backend_id` (cosyvoice-when-installed, `config/dub_backend_id.txt` swappable). **Standalone render PROVEN**: Haerin Korean reference cloned to English, `clone_preserved`, 24kHz/2.64s, report parses into engine `VoiceCloneReport`. `cargo check` green. Remaining: desktop build, end-to-end mp4 proof, self-contained provisioning (install fn + hashed lockfile + offline-bundle capture + wetext pre-cache + render-wrapper shipping). |
| WP-0254 | Lane-based job scheduling + startup subscription auto-sync | IN_PROGRESS | Claude | Operator 2026-06-15: "vv can't do 2 things at once / single videos blocked by playlists; the parallelism setting is global and wrong." Verified root cause: one global FIFO pool (live `jobs_max_concurrency=2`), playlist refresh fans out ~1000 child downloads into the shared queue (jobs.rs:6504) → starves single videos + localization. Replace with per-lane budgets (Single 3 / Recurring 1 / Localization 1), recurring-lane = conservative anti-bot, + 4KVDP-style startup auto-check+auto-download of due subs into the recurring lane. Engine-first; supersedes WP-0165 Quick/Advanced gate for the archiver. WP: `work_packets/WP-0254_JOB_LANES_SCHEDULING_AND_STARTUP_AUTO_SYNC.md`. |
| WP-0255 | Video Archiver redesign + per-lane Options UI + "legacy" terminology purge | SUPERSEDED | Claude | Shipped behavior and proof remain preserved. Every unresolved Video Archiver simplification, progress, control-clarity, and render-bounding requirement is carried forward into WP-0280. Historical contract: `work_packets/WP-0255_VIDEO_ARCHIVER_SUBSCRIPTION_MANAGER_REDESIGN.md`. |
| WP-0257 | Subscription-refresh anti-bot / auth-block cascade hardening | IN_PROGRESS | Claude | Operator 2026-06-17: "Update all subscriptions failed in jobs.queue... use /workflows to analyse and harden". Root cause (live job DB): a burst of 255 channel enumerations tripped YouTube anti-bot; ONE channel's cookie rejection cascaded into a fleet-wide outage because the auth-block is GLOBAL-scoped + STICKY (no TTL) + fail-fast (276 refresh FAILED). Analyzed via 7-agent workflow `wf_4a8044e5-e4d`. DONE (engine + UI + tests, shipped 0.1.78-0.1.80): #1 corroboration (>=3 distinct subs must reject before a global block), #2 TTL + escalating backoff auto-heal (+ old sticky blocks auto-clear on upgrade), #5 don't pin subs in backoff for shared auth-block failures, #3 recurring-lane cooldown + enumeration --sleep-requests (tunable in Options -> Anti-bot pacing), #4 "Update all" trickle (cap, most-overdue first), #6 plain-language failure labels in Jobs. Deferred refinements: per-sub auth-key scope, block->lane-pause circuit-breaker, retry-unresolved guard, engine FailureCategory column + transient auto-retry. WP: `work_packets/WP-0257_SUBSCRIPTION_REFRESH_ANTIBOT_AUTHBLOCK_HARDENING.md`. |
| WP-0256 | Jobs/Queue readable playlist list + reliable video titles across all states | DONE | Codex | Desktop 0.1.96 completed the v2 no-card Jobs rebuild: `Now`, `Needs attention`, and `History`; truthful canonical totals vs bounded rows; readable work/source/progress/timing/actions; explicit loading/error/empty states; durable single-video job-ID receipts; exact operator URL/library/media proof; and clean release bridge snapshots. Proof: `product/desktop/build_target/tool_artifacts/wp_runs/WP-0256/20260716_jobs_rebuild_v2/summary.md`. Refinement: `work_packets/WP-0256_JOBS_QUEUE_READABLE_SOURCE_LINKED_PROGRESS_v2_REFINEMENT.md`. |
| WP-0253 | Library unification + responsiveness + NAS fallback + bundled watcher | IN_PROGRESS | Claude | Operator 2026-06-15 Priority-2 polish (after localization). DONE+tested: 2c single-library v16 migration (additive `origin`+`library_id` columns + backfill, verified on a copy of the real 122k-row DB: 121,090 `4kvdp_import` + 1,349 `voxvulgi_download`, all bound); 2b responsiveness — v16 indexes (unified query now `SEARCH ... USING INDEX idx_library_item_library_created`, no 122k full-scan) + subscription-list 255-stat N+1 → one dir read; 2d NAS local-fallback helper (bounded UNC reachability probe + `effective_download_dir_with_fallback` + always-local fallback dir, additive/no file moves); 2a watcher bundled as installer resource (`watcher/**/*`) + detached supervisor (freeze-survival, crash-relaunch while app-pid alive, stop-flag + pid-gate exit, single-instance + crash-storm guard, safe-mode/config gated) spawned from setup(), build syncs the watcher copy. `cargo test` 217 passed; engine+desktop compile. Shipped in 0.1.69. Remaining: 2c UI list-collapse + new-item stamping, 2b candidates paging/search, 2d live-wiring + operator-confirmed resync, 2a sampling-budget tuning + extra debug signals. |
| WP-0258 | Jobs/Queue query performance + DB-contention stall elimination | SUPERSEDED | Codex | Desktop 0.1.96 shipped the bounded canonical read-path slice and its proof remains preserved. All unresolved p95, retry/repair, archive-stat, render-bound, contention, and verification scope is carried forward into WP-0280. Historical refinement: `work_packets/WP-0258_JOBS_QUERY_PERFORMANCE_DB_CONTENTION_STALLS_v2_REFINEMENT.md`. |
| WP-0259 | Remove legacy wording + unify old/new + move 4KVDP import to Settings | DONE (0.1.82, validated: FE tsc + engine cargo check green) | Claude | Operator 2026-07-01 overnight: "remove all legacy wording; new and old are the same; export must not distinguish; only keep 4K Downloader import, under Settings." Audit-grounded (read-only subagent): legacy groups only created on explicit 4KVDP import (not startup); library export already doesn't leak origin; only leak is group names; big "Legacy archive import" card is dead code (`{false &&}`); Options already hosts the 4KVDP importer. DONE (engine): neutral group-name constants + idempotent db v19 in-place rename of "Legacy 4KVDP*" groups (rename-only, preserves ids+memberships, NOT-EXISTS collision-guarded). Remaining (FE, batched): delete dead legacy card + duplicate LibraryPage importer buttons + rename Options importer labels ("Import from 4K Video Downloader") + fix "Single videos / legacy" filter label. Data preserved (origin column + localStorage keys + command names kept). WP: `work_packets/WP-0259_DELEGACY_UNIFY_OLD_NEW.md`. NO build. |
| WP-0260 | Non-technical UX: plain-language copy + progressive disclosure + light visual overhaul | DONE (0.1.82, validated; follow-ups: Diagnostics Voice-cloning-packages block + LOC_HELP glossary) | Claude | Operator 2026-07-01 overnight: "app for non-technical users; hide technical options more; explainers too technical; light visual overhaul but lightweight." Two audits (copy/disclosure 55 tool-uses + visual critique 7 shots): Table A copy rewrites, Table B controls-to-hide, top-20; key gap = Localization page has no Advanced-collapse despite promising one; Quick/Advanced siloed to archiver. Scope: plain-language copy, `<details>`/Advanced disclosure of Localization mix/mux + Options custom-preset/anti-bot + archiver folder/network knobs, and CSS-light visual (semantic buttons, muted text tier, status badges, zebra tables, de-dup paths, "More" menus, grouped Media filters). No cards, no new deps/timers. WP: `work_packets/WP-0260_NONTECHNICAL_UX_COPY_VISUAL.md`. NO build. |
| WP-0261 | Consumer process signaling + subscription progress observability | DONE (0.1.82, validated: engine trace + /agent/subscriptions_activity + live per-sub rows/detail) | Claude | Operator 2026-07-01 overnight: "no clue what's being processed after Update all; want live moving progress + numeration in the subscription panel (the consumer front-end); Jobs stays technical" + "fix the monitoring blind spot + display it." Design-complete (read-only subagent 49 tool-uses): all data exists; add read-only `youtube_subscriptions_activity()` (join child downloads by batch_id=refresh job_id -> per-sub queued/running/succeeded/failed + current title/progress), a "Processing now" banner + live per-sub counts reusing the existing slow-poll loop (no new timers), engine-side diagnostics-trace events (subscription_refresh_begin/enumerated/done/failed) + a `/agent/subscriptions_activity` bridge endpoint (also fixes agent observability). DONE (engine + FE, tsc + engine cargo check green, NOT built): read-only `youtube_subscriptions_activity()` command (joins child downloads by batch_id) + Tauri registration; FE type + state + loader wired into the existing slow poll + a live "Processing now" banner (moving bar + "X/Y this run" numeration + current title). Remaining: engine trace events + bridge endpoint (2c, agent observability) + per-row/detail live-count enrichment. WP: `work_packets/WP-0261_CONSUMER_PROCESS_SIGNALING.md`. NO build. |
| WP-0262 | Localization Studio: root cause (never worked) + fix plan | MOSTLY DONE (0.1.82: multi-speaker fails visibly + "Make subtitles only" DONE+validated. LIVE venv fixed to compatible pins transformers 4.49.0 / tokenizers 0.21.4 / huggingface_hub 0.34.4 + lockfile made self-consistent with PyPI-verified hashes. kokoro `from kokoro.model import KModel` PROVEN: exit 0 (compatible) + 12.3s wall / 10.8s CPU WARM. The original ~18-min was a ONE-TIME cold `.pyc` compile of freshly-installed pkgs under heavy machine contention, NOT a recurring version hang — reshapes the earlier "transformers-mismatch hang" theory: version was a real COMPATIBILITY bug, cold-compile+load was the wall-time. Import-timeout blocker removed. REMAINING: full end-to-end dub SYNTH not yet run (import proven, not synthesis); CosyVoice warm-import unverified; the eng-deps install-time warmup should pre-compile so first job-time import isn't cold) | Claude | Operator 2026-07-01: "technical deepdive — main feature never worked ever, not subtitles/single(haerin)/multi(miyeon)." Evidence-based deep-dive (121-tool-use subagent) against the LIVE job DB + disk + venv import probes. ROOT CAUSES: (1) subtitles DO produce but garbled with whisper-tiny (WP-0252 large-v3 default fixes, model on disk); (2) LIVE BLOCKER — dub TTS model-class import stalls minutes (kokoro `KModel` ~18min vs transformers 5.8.1 mismatch; CosyVoice `CosyVoice2` >150s) → blows job timeout → no audio; (3) multi-speaker silently stalls at the voice-plan gate (no job rows) — can't build per-speaker refs from chaotic audio; (4) META: WP-0251/0252 were UNCOMMITTED + the "shipped 0.1.68/0.1.69" note was FALSE (no such build) — the 0.1.81 built this session is the first build actually containing them. Fix plan (operator-rebuild-gated for the dep pins; NOT blind-patched — unvalidatable without a venv build): repair kokoro↔transformers pins + instrument the CosyVoice import, surface the multi-speaker voice-plan stall, expose subtitles-only. WP: `work_packets/WP-0262_LOCALIZATION_STUDIO_ROOT_CAUSE_AND_FIX_PLAN.md`. NO build. |
| WP-0263 | Instagram Archiver parity: single + passive subscription cron + global cookie | DONE (0.1.82, validated: engine cargo 226 tests + FE tsc; follow-ups: periodic passive-rotation tick + full WP-0257-style auth-block corroboration/TTL state machine) | Claude | Operator 2026-07-01: "instagram archiver should work like video archiver — single channel download, subscribe to a channel (cron every few hours, VERY passive for Meta's strict anti-bot), instagram cookie pasted in Options and used for everything instagram. add to the list for next release." Grounded (grep): instagram_subscription table + instagram_subscriptions.rs module (list/upsert/queue-one/queue-all + is_subscription_due + per-sub cookie) already exist; gaps = no recurring-lane/startup-auto-sync integration, no dedicated global Instagram cookie in Options (per-sub today), second-class UI. Scope: (2a) single Instagram download surface; (2b) passive cron in the recurring lane + startup auto-sync + EXTRA-conservative Meta pacing (separate keys, long cooldown+jitter, one-at-a-time, hours interval); (2c) global Instagram cookie in Options used everywhere (precedence: per-job -> global -> browser) + Test preflight + WP-0257-style corroboration/TTL auth-block guard; (2d) Instagram subscription manager UI matching WP-0255/0260. Builds on WP-0162/0254/0255/0257/0260. WP: `work_packets/WP-0263_INSTAGRAM_ARCHIVER_PARITY_PASSIVE_CRON_GLOBAL_COOKIE.md`. Next release. |
| WP-0264 | Failure-state telegraphing: clear state + required action in subscription panel AND Jobs/Queue | DONE (0.1.82, validated: engine 226 tests + FE tsc; jobs.rs wiring to persist last_error_message included) | Claude | Operator 2026-07-01: "when youtube is rate limiting or needs a sign in/cookie it should show the different states + requirements in BOTH the subscription panel and jobs/queue. it is now very unclear and badly telegraphed." Grounded in live DB taxonomy (corrected by HTTP status): the current 45 "need attention" = 23 db-lock (busy, fixed 0.1.82), 16 HTTP-404 handle-renamed/gone (e.g. @mirai42322 videos+shorts, never worked in 116 days), 2 no-videos-tab, 3 tool, 1 file-busy — i.e. ~18 are stale/wrong handles, NOT rate-limits/sign-in (my first read mis-truncated 404 as rate-limit). Design: one FE classifier (failureStates.ts, status-code-aware: 404->handle-not-found/update-URL, 403->sign-in, 429->rate-limited, db-lock->busy) + engine persistence (schema v21 `last_error_message` written on refresh failure, cleared on success, returned on the row). FE DONE (tsc green): per-sub chip+requirement in list+detail, per-kind status-strip breakdown, Jobs chip+requirement with raw error behind expander. Engine (v21 column + record failure/success + list/group query) landing. WP: `work_packets/WP-0264_FAILURE_STATE_TELEGRAPHING_SUBS_AND_JOBS.md`. For next build. |
| WP-0265 | Installer packaging fix (thin NSIS) + full-offline packaging plan | IN_PROGRESS | Claude | Operator 2026-07-08: new installer attempt with a lightweight model "got stuck for some unknown reason"; hunch "reinstall all dependencies". Verified root cause: code compiled fine every time; the ~54min hang + `failed to run light.exe` was WiX MSI packaging the ~5.08GB bundled offline payload (`offline/**/*`, bundle `offline_full_win64_20260706_213832`) with `targets:"all"` — above the ~2GB WiX/NSIS packaging ceiling the project already documented at 0.1.87. Disk NOT the cause (D: ~1012GB free). The "reinstall deps" memory = an EARLIER payload refresh 2026-07-06 ~21:38; the stuck runs reused that cache (fingerprint matched, no reinstall) and hung at MSI packaging. Operator chose "thin now + fix root cause". DONE (this session): `tauri.conf.json` resources drop `offline/**/*` (keep watcher + voice_backends_seed), `targets` all->nsis; thin NSIS build 0.1.90->0.1.91 via `build_desktop_target.ps1 -NoArchiveCurrent -- --bundles nsis` (reused cached payload, warmup gate skipped). Follow-on DONE this session (operator chose single self-installing ~12GB .exe, full quality incl CosyVoice): full-quality OFFLINE installer built via Inno Setup 6.7.3 (`installer/VoxVulgi_offline_full.iss` + `scripts/build_offline_full_installer.ps1`) wrapping the 8.6MB NSIS setup.exe (`/S`, maintenance flow kept) + a 12.83GB relocatable payload -> per-user `%APPDATA%`. Payload rebuilt from the LIVE working install via robocopy (dereferences the HF symlinks the old export dropped — the real root cause of the broken Kokoro cache); pack roots only, no personal data. Installer rewrites both `pyvenv.cfg` files + places the wetext cache. Venvs proven relocatable (pyvenv.cfg-only). CosyVoice offline gap FOUND+FIXED (WP-0252 "wetext pre-cache"): `wetext.py`->`local_files_only=True` + bundled 20.86MB modelscope cache (no app rebuild; modelscope has no offline env var). Offline audit under dead-proxy PASSED: Kokoro synth, OpenVoice load, spleeter separate, CosyVoice load; whisper C++/local; demucs pre-broken on live too (torchaudio, non-default). Remaining: final clean-machine end-to-end install proof (can't run on this machine without clobbering the live install). WP: `work_packets/WP-0265_INSTALLER_PACKAGING_THIN_NSIS_AND_OFFLINE_PLAN.md`. |
| WP-0266 | YouTube browser-session link and recurring auth guard | DONE | Codex | Shipped and app-boundary verified in 0.1.99: Firefox `Connect and test` accepts the exact reported URL; current browser auth overrides stale queued-job cookies; recurring defaults are 60s + 0-60s jitter, 2s request sleep, 5-10s download sleep, concurrency/fragments 1, update-all 25; auth circuit holds rather than cancels; failed rows lead with classified reasons including Sign-in needed and Stalled. 236 engine tests passed. Proof: `product/desktop/build_target/tool_artifacts/wp_runs/WP-0266/2026-07-16_youtube_auth_jobs_pacing_v0_1_99/summary.md`. WP: `work_packets/WP-0266_YOUTUBE_BROWSER_SESSION_LINK_AND_RECURRING_GUARD.md`. |
| WP-0267 | YouTube sign-in and recovery wizard | DONE | Codex | Shipped and app-boundary verified in 0.1.101: selected normal-browser sign-in, exact-source verification, persisted Ready/Sign-in-required truth, five-step failure recovery, auth-circuit queue holding, and advanced manual-cookie fallback. Exact reported URL passed with Firefox. Proof: `../../product/desktop/build_target/tool_artifacts/wp_runs/WP-0267/2026-07-16_youtube_sign_in_recovery_v0_1_101/summary.md`; WP: `work_packets/WP-0267_YOUTUBE_SIGN_IN_RECOVERY_WIZARD.md`; refinement: `work_packets/WP-0267_YOUTUBE_SIGN_IN_RECOVERY_WIZARD_v1_REFINEMENT.md`. |
| WP-0268 | Canonical archive lineage and single-video history | DONE | Codex | Shipped and app-boundary verified in v0.1.105. Live schema-v25 proof: 44 canonical singles; 1,349 ambiguous older YouTube items preserved outside the single list; 6,113 mapped subscription items excluded; zero subscription-linked rows in single history. Proof: `../../product/desktop/build_target/tool_artifacts/wp_runs/WP-0268/summary.md`; WP: `work_packets/WP-0268_CANONICAL_ARCHIVE_LINEAGE_AND_SINGLE_HISTORY_v1.md`. |
| WP-0269 | Independent service tracks and shared YouTube safety gate | DONE | Codex | Shipped in v0.1.105. Exact live foreground job succeeded while a background YouTube transfer ran against a 40,310-job recurring backlog; both app-owned yt-dlp processes used one fragment and safe 8/9-second sleeps through the shared gate. Proof: `../../product/desktop/build_target/tool_artifacts/wp_runs/WP-0269/summary.md`; WP: `work_packets/WP-0269_INDEPENDENT_SERVICE_TRACKS_AND_SHARED_YOUTUBE_GATE_v1.md`. |
| WP-0270 | Track controls, queue truth, and diagnostics | DONE | Codex | Shipped and app-boundary verified in v0.1.105. Six canonical tracks, scheduler-consumed controls, zero active unclassified rows, shared YouTube gate, direct-transfer budget wording, hidden bridge navigation, inspected screenshots, and zero console errors. Proof: `../../product/desktop/build_target/tool_artifacts/wp_runs/WP-0270/summary.md`; WP: `work_packets/WP-0270_TRACK_CONTROLS_QUEUE_TRUTH_AND_DIAGNOSTICS_v1.md`. |
| WP-0271 | Canonical job track tabs and source identity | DONE | Codex | Shipped in desktop v0.1.107: backend-selected Jobs subtabs for All, YouTube singles, Subscriptions, Instagram, Other videos, Image Archive, Localization, and legacy Unclassified; durable source channel/playlist/page snapshots across job states. Proof: `product/desktop/build_target/tool_artifacts/wp_runs/WP-0271/summary.md`. WP: `work_packets/WP-0271_CANONICAL_JOB_TRACK_TABS_AND_SOURCE_IDENTITY_v1.md`. |
| WP-0272 | Single-video batch activity and live progress | DONE | Codex | Shipped in desktop v0.1.107: canonical queued/downloading single members, paged activity, stable-ID element polling, throttled real yt-dlp progress, and targeted terminal history refresh. Proof: `product/desktop/build_target/tool_artifacts/wp_runs/WP-0272/summary.md`. WP: `work_packets/WP-0272_SINGLE_VIDEO_BATCH_ACTIVITY_AND_LIVE_PROGRESS_v1.md`. |
| WP-0273 | Canonical source dedup and missing-media repair | DONE | Codex | Shipped in desktop v0.1.107: canonical source identity/associations, ordered batch preflight and atomic claims, present/active prevention, missing/unreachable distinction, relocate, approved redownload, verified replacement URL, and metadata-only removal. Proof: `product/desktop/build_target/tool_artifacts/wp_runs/WP-0273/summary.md`. WP: `work_packets/WP-0273_CANONICAL_SOURCE_DEDUP_AND_MISSING_MEDIA_REPAIR_v1.md`. |
| WP-0274 | Granular UI refresh and storage-contention hardening | DONE | Codex | Shipped in desktop v0.1.107: lightweight progress projections, split adaptive polling, visibility-gated heavy reads, bounded cached storage observation, and independent lane execution with shared conservative YouTube start pacing. Proof: `product/desktop/build_target/tool_artifacts/wp_runs/WP-0274/summary.md`. WP: `work_packets/WP-0274_GRANULAR_UI_REFRESH_AND_STORAGE_CONTENTION_HARDENING_v1.md`. |
| WP-0275 | Imported YouTube identity and source membership | DONE | Codex | Schema v27, read-only exact-evidence enrichment, resumable checkpoints, and many-to-many playlist/`videos`/`shorts`/channel memberships passed focused final-state tests and v0.1.113 packaged visual/build proof. Refinement: `work_packets/WP-0275_IMPORTED_YOUTUBE_IDENTITY_AND_SOURCE_MEMBERSHIP_v1_REFINEMENT.md`. |
| WP-0276 | Unified library and queue duplicate prevention | DONE | Codex | Membership-preserving canonical enqueue and paged full-canonical-set queue reconciliation passed focused tests; v0.1.113 packaged Jobs, Library, Video Archiver, and Options surfaces were inspected through the headless bridge. Refinement: `work_packets/WP-0276_UNIFIED_LIBRARY_AND_QUEUE_DUPLICATE_PREVENTION_v1_REFINEMENT.md`. |
| WP-0277 | Recoverable NAS duplicate cleanup | REVIEW | Codex | Schema v28 bounded inventory, staged exact hashes, keeper review, quarantine, relink, and rollback pass disposable-fixture tests and v0.1.113 build/visual proof. Complete packaged UI approval/quarantine/rollback smoke remains; no operator media was mutated. Refinement: `work_packets/WP-0277_RECOVERABLE_NAS_DUPLICATE_CLEANUP_v1_REFINEMENT.md`. |
| WP-0278 | Panel switch and watcher contention hardening | DONE | Codex | The v0.1.109 115,366 ms synchronous-handler failure was corrected. Rebuilt proof delivered all panel targets; the v0.1.112 watcher recorded no slow-panel, freeze/skew, bridge-failure, or incomplete-command events, and final v0.1.113 built/rendered cleanly. No unrelated process was stopped or reconfigured. Refinement: `work_packets/WP-0278_PANEL_SWITCH_AND_WATCHER_CONTENTION_HARDENING_v1_REFINEMENT.md`. |
| WP-0279 | Headless UI audit interaction bridge | DONE | Codex | Shipped and app-boundary verified on v0.1.116: semantic inventory, allowlisted structural navigation, diagnostics receipts, PID-owned bridge discovery, and mutation-free headless startup. Named subscription selection and Jobs expansion passed; final `vvwatch` had zero unresponsive/bridge-failure/DB-timeout/incomplete-command samples. Proof: `product/desktop/build_target/tool_artifacts/wp_runs/WP-0279/final_proof/summary.md`. Refinement: `work_packets/WP-0279_HEADLESS_UI_AUDIT_INTERACTION_BRIDGE_v1_REFINEMENT.md`. |
| WP-0280 | Video Archiver and Jobs cohesive workspaces | DONE | Codex | Shipped in governed desktop v0.1.126: one cohesive Video Archiver workflow selector, bounded subscription/source and Jobs windows with truthful canonical totals, compact shell, canonical Work source selector, disclosed scheduler health, active-drain activity query, attributable non-blocking batch retry/repair receipts, and an independently loaded full-library unclassified diagnostic so Single videos navigation stays fast. Final proof: contracts 93/93; focused receipt/lineage tests; desktop+NSIS build; inspected 800×600 snapshots/dumps with zero missing accessible names; cold Single tab 8 ms, canonical history 9 ms, secondary count 408 ms; Jobs overview p95 56 ms across 23 calls; archive stats 73 ms; 22-sample `vvwatch` with zero unresponsive/bridge/DB/path/freeze/skew failures and no final incomplete command. Build-changelog artifact paths are now existence-gated. Proof: `product/desktop/build_target/tool_artifacts/wp_runs/WP-0280/20260727_v0_1_126/summary.md`. WP: `work_packets/WP-0280_VIDEO_ARCHIVER_JOBS_COHESIVE_WORKSPACES_v1.md`. |
| WP-0281 | Subscription source priority and membership backfill | DONE | Codex | Shipped in v0.1.127: channel-page, `/videos`, and `/shorts` sources run before playlists within the same refresh cohort, with canonical active/present claims as the final anti-loss duplicate gate. Schema v29 additively backfilled 53,224 historical associations into memberships; no NAS media, subtitles, subscriptions, or jobs changed. Proof: `product/desktop/build_target/tool_artifacts/wp_runs/WP-0281/20260727_v0_1_127/summary.md`. Refinement: `work_packets/WP-0281_SUBSCRIPTION_SOURCE_PRIORITY_AND_MEMBERSHIP_BACKFILL_v1_REFINEMENT.md`. |
| WP-0282 | Subscription deleted and unavailable status | DONE | Codex | Manual-only deleted state, exact-404 unavailable state, queue/execution gates, operator + assistant controls, preserved metadata, and live marking of the three verified channels. Proof: `product/desktop/build_target/tool_artifacts/wp_runs/WP-0282/20260727_v0_1_128/summary.md`. |
| WP-0283 | Canonical queued identity compaction and execution gate | IN_PROGRESS | Codex | Corrects the unproven queued-versus-queued gap in WP-0276: full canonical dry-run/apply, deterministic keeper, durable memberships/history, and last-moment execution ownership before network/yt-dlp. Queue stays paused. Refinement: `work_packets/WP-0283_CANONICAL_QUEUED_IDENTITY_COMPACTION_AND_EXECUTION_GATE_v1_REFINEMENT.md`. |
| WP-0284 | Subscription video selection, deletion, and manual redownload | DONE | Codex | Shipped in desktop v0.1.132: exact stable-ID single/bulk selection in Video Archiver and Media Library; recoverable/permanent file deletion with preserved canonical metadata; durable operator-deleted suppression across automatic/retry/redownload-all paths; only exact explicitly authorized manual jobs may rematerialize. Queue remains paused. Proof: `product/desktop/build_target/tool_artifacts/wp_runs/WP-0284/summary.md`. Refinement: `work_packets/WP-0284_SUBSCRIPTION_VIDEO_SELECTION_DELETE_AND_MANUAL_REDOWNLOAD_v1_REFINEMENT.md`. |
| WP-0285 | NAS deleted-space reclamation incident and evidence handoff | IN_PROGRESS | Codex | Plan B materially succeeded: one verified DSM restart changed Volume 1 from `55.7 TB` used / `132.7 GB` free to a stabilized `45.3 TB` used / `10.5 TB` free. Usage Details showed Shared Folder unchanged at `29 TB` while Synology Drive fell from `26.7 TB` to `16.3 TB`, proving Drive as the released owner category. All four drives remained Healthy; no second restart, rescan, package action, scrub, balance, repair, schedule change, or deletion occurred. The approximately `10.37 TB` displayed gain is rounded because an exact-byte UNC probe timed out. Immediate capacity target exceeded; residual `16.3 TB` Drive attribution is preserved for Support only if the operator wants it explained. WP: `work_packets/WP-0285_NAS_DELETED_SPACE_RECLAMATION_INCIDENT_v1.md`. |
| WP-0286 | Canonical Media Library unification and live imported-identity enrichment | DONE | Codex | Shipped in desktop v0.1.133: the full canonical Media Library set is filtered/searched/sorted before pagination with truthful totals; 37,631 additional exact imported YouTube links were safely applied after a verified live DB backup; unresolved evidence and all NAS media were preserved. Quiet headless proof rendered real NAS rows for YouTube + Video + Available and reported 49,942 matching items. Proof: `product/desktop/build_target/tool_artifacts/wp_runs/WP-0286/summary.md`. Refinement: `work_packets/WP-0286_CANONICAL_MEDIA_LIBRARY_UNIFICATION_AND_LIVE_IMPORTED_IDENTITY_ENRICHMENT_v1_REFINEMENT.md`. |
| WP-0287 | Localization stack revision research and landscape refresh | DONE | Claude | Governance-only packet, complete 2026-08-01. 6 web-research lanes (ASR, translate, diarize, separate, voice-clone TTS, 12 field pipelines) with code+weights licenses verified separately at primary sources, synthesized into `governance/spec/LOCALIZATION_STACK_LANDSCAPE_2026_07.md` (8 topics final); 2026-02 landscape doc marked SUPERSEDED. Recommended stack: whisper large-v3 (KO) + Qwen3-ASR-1.7B (JA) + ForcedAligner; llama.cpp LLM translation (Qwen3.5-35B-A3B) replacing whisper-translate; sherpa-onnx diarization (pyannote seg-3.0 MIT + CAM++ Apache) replacing resemblyzer; Kim Mel-Band RoFormer (MIT) via single-process python-audio-separator replacing Spleeter; Fun-CosyVoice3 direct cross-lingual cloning replacing the Kokoro+OpenVoice cascade (GPT-SoVITS MIT runner-up/CPU lane); watermarked backends excluded per guardrail. All 4 operator decisions resolved 2026-08-01: benchmark-first + backup registry, minimum-hardware contract agreed (PRODUCT_SPEC 8.1.9), ungated CC-BY-4.0 diarization mirror accepted, closure unit folded into WP-0289. No product code changed. Proof: `product/desktop/build_target/tool_artifacts/wp_runs/WP-0287/20260801_research_complete/summary.md`. WP: `work_packets/WP-0287_LOCALIZATION_STACK_REVISION_RESEARCH_AND_LANDSCAPE_REFRESH_v1.md`. |
| WP-0288 | Localization stack local benchmark and backup registry | BACKLOG | — | Measures the WP-0287 candidates on real operator content (Haerin single-speaker, Miyeon multi-speaker, plus KO/JA clips) and freezes per-stage defaults from evidence rather than research. Includes the packaging gate every bundle-eligible candidate must pass: clean py3.11 venv from local wheelhouse (`pip --no-index`), pre-populated HF cache, `HF_HUB_OFFLINE=1`, network actually blocked, run to first output, full model-graph asset enumeration. Deliverable also includes a durable ranked backup registry per stage (operator decision: "record other methods as back ups"). Blocks WP-0289. WP: `work_packets/WP-0288_LOCALIZATION_STACK_LOCAL_BENCHMARK_AND_BACKUP_REGISTRY_v1.md`. |
| WP-0289 | Revised stack: first dubbed deliverable through the installed app | BACKLOG | — | Vertical slice implementing the benchmark-frozen stack plus the batteries-included payload (PRODUCT_SPEC 8.1.8) and tier detection (8.1.9). **Carries the folded closure unit**: one dubbed MP4 of the Haerin clip produced through the *installed* app with the network blocked, landing in the localization export root with a truthful clone-status label, plus the Miyeon multi-speaker case completing or failing visibly. Historical baseline to beat: 0 mixes, 0 muxes, 0 exported deliverables across 129,077 derived item folders. 10 microtasks; manual/UI-heavy proof class (PROOF_STANDARD 3.4); changelog entry must carry a real commit hash. Advanced surfaces explicitly frozen. WP: `work_packets/WP-0289_REVISED_STACK_FIRST_DUBBED_DELIVERABLE_THROUGH_INSTALLED_APP_v1.md`. Refinement: `work_packets/WP-0289_REVISED_STACK_FIRST_DUBBED_DELIVERABLE_THROUGH_INSTALLED_APP_v1_REFINEMENT.md`. |

## OPERATOR AUTHORITY (pace / scope / stopping)

- The assistant/agent is FORBIDDEN to decide pace, scope, or when it stops working. The operator alone decides scope, pace, and when work stops. No deferring, subsetting, reprioritizing, pausing, or declaring "done for now" without explicit operator say-so; when the operator lists multiple requirements, implement ALL of them.
